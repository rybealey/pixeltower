diff --git a/src/api/avatar/AvatarEditorUtilities.ts b/src/api/avatar/AvatarEditorUtilities.ts
index 3626ab8..9b318c9 100644
--- a/src/api/avatar/AvatarEditorUtilities.ts
+++ b/src/api/avatar/AvatarEditorUtilities.ts
@@ -70,7 +70,9 @@ export class AvatarEditorUtilities
 
         const partColors: IPartColor[] = new Array(colorIds.length);
         const clubItemsDimmed = this.clubItemsDimmed;
-        const clubMemberLevel = GetClubMemberLevel();
+        // pixeltower: HC unlocked — every user is treated as max club tier so
+        // the isDisabled flag never trips CategoryBaseModel.selectPart's revert.
+        const clubMemberLevel = 2;
 
         for(const partColor of palette.colors.getValues())
         {
@@ -257,7 +259,8 @@ export class AvatarEditorUtilities
 
         for(const color of palette.colors.getValues())
         {
-            if(!color.isSelectable || (GetClubMemberLevel() < color.clubLevel)) continue;
+            // pixeltower: HC unlocked — drop the clubLevel gate.
+            if(!color.isSelectable) continue;
 
             return color.id;
         }
diff --git a/src/components/avatar-editor/AvatarEditorView.tsx b/src/components/avatar-editor/AvatarEditorView.tsx
index 6b3766e..a917b08 100644
--- a/src/components/avatar-editor/AvatarEditorView.tsx
+++ b/src/components/avatar-editor/AvatarEditorView.tsx
@@ -129,7 +129,8 @@ export const AvatarEditorView: FC<{}> = props =>
                 resetCategories();
                 return;
             case AvatarEditorAction.ACTION_RANDOMIZE:
-                const figure = generateRandomFigure(figureData, figureData.gender, GetClubMemberLevel(), figureSetIds, [ FigureData.FACE ]);
+                // pixeltower: HC unlocked — random figures pull from the full part set.
+                const figure = generateRandomFigure(figureData, figureData.gender, 2, figureSetIds, [ FigureData.FACE ]);
 
                 loadAvatarInEditor(figure, figureData.gender, false);
                 resetCategories();
diff --git a/src/components/avatar-editor/views/AvatarEditorWardrobeView.tsx b/src/components/avatar-editor/views/AvatarEditorWardrobeView.tsx
index 9811ab8..2ec0984 100644
--- a/src/components/avatar-editor/views/AvatarEditorWardrobeView.tsx
+++ b/src/components/avatar-editor/views/AvatarEditorWardrobeView.tsx
@@ -58,11 +58,11 @@ export const AvatarEditorWardrobeView: FC<AvatarEditorWardrobeViewProps> = props
                     { figureContainer &&
                     <LayoutAvatarImageView figure={ figureContainer.getFigureString() } gender={ gender } direction={ 2 } /> }
                     <Base className="avatar-shadow" />
-                    { !hcDisabled && (clubLevel > 0) && <LayoutCurrencyIcon className="position-absolute top-1 start-1" type="hc" /> }
+                    { /* pixeltower: HC unlocked — drop wardrobe HC badge + Use-button gate. */ }
                     <Flex gap={ 1 } className="button-container">
                         <Button variant="link" fullWidth onClick={ event => saveFigureAtWardrobeIndex(index) }>{ LocalizeText('avatareditor.wardrobe.save') }</Button>
                         { figureContainer &&
-                        <Button variant="link" fullWidth onClick={ event => wearFigureAtIndex(index) } disabled={ (clubLevel > GetClubMemberLevel()) }>{ LocalizeText('generic_usable.button.use') }</Button> }
+                        <Button variant="link" fullWidth onClick={ event => wearFigureAtIndex(index) }>{ LocalizeText('generic_usable.button.use') }</Button> }
                     </Flex>
                 </LayoutGridItem>
             );
diff --git a/src/components/avatar-editor/views/figure-set/AvatarEditorFigureSetItemView.tsx b/src/components/avatar-editor/views/figure-set/AvatarEditorFigureSetItemView.tsx
index fd28dc5..71cfeab 100644
--- a/src/components/avatar-editor/views/figure-set/AvatarEditorFigureSetItemView.tsx
+++ b/src/components/avatar-editor/views/figure-set/AvatarEditorFigureSetItemView.tsx
@@ -1,6 +1,6 @@
 import { FC, useEffect, useState } from 'react';
-import { AvatarEditorGridPartItem, GetConfiguration } from '../../../../api';
-import { LayoutCurrencyIcon, LayoutGridItem, LayoutGridItemProps } from '../../../../common';
+import { AvatarEditorGridPartItem } from '../../../../api';
+import { LayoutGridItem, LayoutGridItemProps } from '../../../../common';
 import { AvatarEditorIcon } from '../AvatarEditorIcon';
 
 export interface AvatarEditorFigureSetItemViewProps extends LayoutGridItemProps
@@ -13,8 +13,6 @@ export const AvatarEditorFigureSetItemView: FC<AvatarEditorFigureSetItemViewProp
     const { partItem = null, children = null, ...rest } = props;
     const [ updateId, setUpdateId ] = useState(-1);
 
-    const hcDisabled = GetConfiguration<boolean>('hc.disabled', false);
-
     useEffect(() =>
     {
         const rerender = () => setUpdateId(prevValue => (prevValue + 1));
@@ -26,7 +24,7 @@ export const AvatarEditorFigureSetItemView: FC<AvatarEditorFigureSetItemViewProp
 
     return (
         <LayoutGridItem itemImage={ (partItem.isClear ? undefined : partItem.imageUrl) } itemActive={ partItem.isSelected } { ...rest }>
-            { !hcDisabled && partItem.isHC && <LayoutCurrencyIcon className="position-absolute end-1 bottom-1" type="hc" /> }
+            { /* pixeltower: HC unlocked — HC badge dropped from figure-set items. */ }
             { partItem.isClear && <AvatarEditorIcon icon="clear" /> }
             { partItem.isSellable && <AvatarEditorIcon icon="sellable" position="absolute" className="end-1 bottom-1" /> }
             { children }
diff --git a/src/components/avatar-editor/views/palette-set/AvatarEditorPaletteSetItemView.tsx b/src/components/avatar-editor/views/palette-set/AvatarEditorPaletteSetItemView.tsx
index 638a9d1..4e8117f 100644
--- a/src/components/avatar-editor/views/palette-set/AvatarEditorPaletteSetItemView.tsx
+++ b/src/components/avatar-editor/views/palette-set/AvatarEditorPaletteSetItemView.tsx
@@ -1,6 +1,6 @@
 import { FC, useEffect, useState } from 'react';
-import { AvatarEditorGridColorItem, GetConfiguration } from '../../../../api';
-import { LayoutCurrencyIcon, LayoutGridItem, LayoutGridItemProps } from '../../../../common';
+import { AvatarEditorGridColorItem } from '../../../../api';
+import { LayoutGridItem, LayoutGridItemProps } from '../../../../common';
 
 export interface AvatarEditorPaletteSetItemProps extends LayoutGridItemProps
 {
@@ -12,8 +12,6 @@ export const AvatarEditorPaletteSetItem: FC<AvatarEditorPaletteSetItemProps> = p
     const { colorItem = null, children = null, ...rest } = props;
     const [ updateId, setUpdateId ] = useState(-1);
 
-    const hcDisabled = GetConfiguration<boolean>('hc.disabled', false);
-
     useEffect(() =>
     {
         const rerender = () => setUpdateId(prevValue => (prevValue + 1));
@@ -25,7 +23,7 @@ export const AvatarEditorPaletteSetItem: FC<AvatarEditorPaletteSetItemProps> = p
 
     return (
         <LayoutGridItem itemHighlight itemColor={ colorItem.color } itemActive={ colorItem.isSelected } className="clear-bg" { ...rest }>
-            { !hcDisabled && colorItem.isHC && <LayoutCurrencyIcon className="position-absolute end-1 bottom-1" type="hc" /> }
+            { /* pixeltower: HC unlocked — HC badge dropped from palette-set items. */ }
             { children }
         </LayoutGridItem>
     );
