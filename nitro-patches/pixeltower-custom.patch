diff --git a/index.html b/index.html
index dd4538b..d3b5e43 100644
--- a/index.html
+++ b/index.html
@@ -23,7 +23,7 @@
     <div id="root" class="w-100 h-100"></div>
     <script>
       const NitroConfig = {
-        "config.urls": [ '/renderer-config.json', '/ui-config.json' ],
+        "config.urls": [ 'renderer-config.json', 'ui-config.json' ],
         "sso.ticket": (new URLSearchParams(window.location.search).get('sso') || null),
         "forward.type": (new URLSearchParams(window.location.search).get('room') ? 2 : -1),
         "forward.id": (new URLSearchParams(window.location.search).get('room') || 0),
diff --git a/src/App.tsx b/src/App.tsx
index 072b05d..fd5b79f 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,6 +1,7 @@
 import { ConfigurationEvent, GetAssetManager, HabboWebTools, LegacyExternalInterface, Nitro, NitroCommunicationDemoEvent, NitroConfiguration, NitroEvent, NitroLocalizationEvent, NitroVersion, RoomEngineEvent } from '@nitrots/nitro-renderer';
 import { FC, useCallback, useEffect, useState } from 'react';
 import { GetCommunication, GetConfiguration, GetNitroInstance, GetUIVersion } from './api';
+import { registerPixeltowerMessages } from './api/pixeltower/PixeltowerSoundMessages';
 import { Base, TransitionAnimation, TransitionAnimationTypes } from './common';
 import { LoadingView } from './components/loading/LoadingView';
 import { MainView } from './components/main/MainView';
@@ -87,6 +88,7 @@ export const App: FC<{}> = props =>
                 if(status)
                 {
                     GetCommunication().init();
+                    registerPixeltowerMessages();
 
                     setPercent(prevValue => (prevValue + 20))
                 }
diff --git a/src/api/pixeltower/PixeltowerSoundMessages.ts b/src/api/pixeltower/PixeltowerSoundMessages.ts
new file mode 100644
index 0000000..a3db6f9
--- /dev/null
+++ b/src/api/pixeltower/PixeltowerSoundMessages.ts
@@ -0,0 +1,290 @@
+import { IMessageComposer, IMessageConfiguration, IMessageDataWrapper, IMessageEvent, IMessageParser, MessageEvent } from '@nitrots/nitro-renderer';
+import { GetConnection } from '../nitro/GetConnection';
+
+const PIXELTOWER_PLAY_SOUND = 6500;
+const PIXELTOWER_UPDATE_PLAYER_STATS = 6501;
+const PIXELTOWER_UPDATE_TARGET_STATS = 6502;
+const PIXELTOWER_OPEN_CLIENT_LINK = 6503;
+const PIXELTOWER_SET_TARGET = 6550;
+
+// ──────────── PlaySound (header 6500) ────────────
+
+export class NitroPlaySoundParser implements IMessageParser
+{
+    private _sample: string;
+
+    public flush(): boolean
+    {
+        this._sample = null;
+        return true;
+    }
+
+    public parse(wrapper: IMessageDataWrapper): boolean
+    {
+        if(!wrapper) return false;
+        this._sample = wrapper.readString();
+        return true;
+    }
+
+    public get sample(): string
+    {
+        return this._sample;
+    }
+}
+
+export class NitroPlaySoundEvent extends MessageEvent implements IMessageEvent
+{
+    constructor(callBack: Function)
+    {
+        super(callBack, NitroPlaySoundParser);
+    }
+
+    public getParser(): NitroPlaySoundParser
+    {
+        return this.parser as NitroPlaySoundParser;
+    }
+}
+
+// ──────────── UpdatePlayerStats (header 6501) ────────────
+
+export class UpdatePlayerStatsParser implements IMessageParser
+{
+    private _hp: number;
+    private _maxHp: number;
+    private _energy: number;
+    private _maxEnergy: number;
+    private _level: number;
+    private _xp: number;
+    private _skillPointsUnspent: number;
+    private _skillHit: number;
+    private _skillEndurance: number;
+    private _skillStamina: number;
+
+    public flush(): boolean
+    {
+        this._hp = 0;
+        this._maxHp = 0;
+        this._energy = 0;
+        this._maxEnergy = 0;
+        this._level = 0;
+        this._xp = 0;
+        this._skillPointsUnspent = 0;
+        this._skillHit = 0;
+        this._skillEndurance = 0;
+        this._skillStamina = 0;
+        return true;
+    }
+
+    public parse(wrapper: IMessageDataWrapper): boolean
+    {
+        if(!wrapper) return false;
+        this._hp = wrapper.readInt();
+        this._maxHp = wrapper.readInt();
+        this._energy = wrapper.readInt();
+        this._maxEnergy = wrapper.readInt();
+        this._level = wrapper.readInt();
+        this._xp = wrapper.readInt();
+        this._skillPointsUnspent = wrapper.readInt();
+        this._skillHit = wrapper.readInt();
+        this._skillEndurance = wrapper.readInt();
+        this._skillStamina = wrapper.readInt();
+        return true;
+    }
+
+    public get hp(): number { return this._hp; }
+    public get maxHp(): number { return this._maxHp; }
+    public get energy(): number { return this._energy; }
+    public get maxEnergy(): number { return this._maxEnergy; }
+    public get level(): number { return this._level; }
+    public get xp(): number { return this._xp; }
+    public get skillPointsUnspent(): number { return this._skillPointsUnspent; }
+    public get skillHit(): number { return this._skillHit; }
+    public get skillEndurance(): number { return this._skillEndurance; }
+    public get skillStamina(): number { return this._skillStamina; }
+}
+
+export class UpdatePlayerStatsEvent extends MessageEvent implements IMessageEvent
+{
+    constructor(callBack: Function)
+    {
+        super(callBack, UpdatePlayerStatsParser);
+    }
+
+    public getParser(): UpdatePlayerStatsParser
+    {
+        return this.parser as UpdatePlayerStatsParser;
+    }
+}
+
+// ──────────── UpdateTargetStats (header 6502) ────────────
+
+export class UpdateTargetStatsParser implements IMessageParser
+{
+    private _habboId: number;
+    private _figure: string;
+    private _userName: string;
+    private _hp: number;
+    private _maxHp: number;
+    private _energy: number;
+    private _maxEnergy: number;
+    private _level: number;
+    private _xp: number;
+    private _skillPointsUnspent: number;
+    private _skillHit: number;
+    private _skillEndurance: number;
+    private _skillStamina: number;
+
+    public flush(): boolean
+    {
+        this._habboId = 0;
+        this._figure = '';
+        this._userName = '';
+        this._hp = 0;
+        this._maxHp = 0;
+        this._energy = 0;
+        this._maxEnergy = 0;
+        this._level = 0;
+        this._xp = 0;
+        this._skillPointsUnspent = 0;
+        this._skillHit = 0;
+        this._skillEndurance = 0;
+        this._skillStamina = 0;
+        return true;
+    }
+
+    public parse(wrapper: IMessageDataWrapper): boolean
+    {
+        if(!wrapper) return false;
+        this._habboId = wrapper.readInt();
+        this._figure = wrapper.readString();
+        this._userName = wrapper.readString();
+        this._hp = wrapper.readInt();
+        this._maxHp = wrapper.readInt();
+        this._energy = wrapper.readInt();
+        this._maxEnergy = wrapper.readInt();
+        this._level = wrapper.readInt();
+        this._xp = wrapper.readInt();
+        this._skillPointsUnspent = wrapper.readInt();
+        this._skillHit = wrapper.readInt();
+        this._skillEndurance = wrapper.readInt();
+        this._skillStamina = wrapper.readInt();
+        return true;
+    }
+
+    public get habboId(): number { return this._habboId; }
+    public get figure(): string { return this._figure; }
+    public get userName(): string { return this._userName; }
+    public get hp(): number { return this._hp; }
+    public get maxHp(): number { return this._maxHp; }
+    public get energy(): number { return this._energy; }
+    public get maxEnergy(): number { return this._maxEnergy; }
+    public get level(): number { return this._level; }
+    public get xp(): number { return this._xp; }
+    public get skillPointsUnspent(): number { return this._skillPointsUnspent; }
+    public get skillHit(): number { return this._skillHit; }
+    public get skillEndurance(): number { return this._skillEndurance; }
+    public get skillStamina(): number { return this._skillStamina; }
+}
+
+export class UpdateTargetStatsEvent extends MessageEvent implements IMessageEvent
+{
+    constructor(callBack: Function)
+    {
+        super(callBack, UpdateTargetStatsParser);
+    }
+
+    public getParser(): UpdateTargetStatsParser
+    {
+        return this.parser as UpdateTargetStatsParser;
+    }
+}
+
+// ──────────── OpenClientLink (header 6503) ────────────
+//
+// Generic "open Nitro UI X" composer. Body is a single string consumed by
+// PixeltowerLinkListener via CreateLinkEvent(uri). Sent server-side from the
+// rp_functional furniture dispatcher; allow-listed by prefix on the client
+// to keep misconfigured DB rows from opening arbitrary surfaces.
+
+export class OpenClientLinkParser implements IMessageParser
+{
+    private _uri: string;
+
+    public flush(): boolean
+    {
+        this._uri = null;
+        return true;
+    }
+
+    public parse(wrapper: IMessageDataWrapper): boolean
+    {
+        if(!wrapper) return false;
+        this._uri = wrapper.readString();
+        return true;
+    }
+
+    public get uri(): string
+    {
+        return this._uri;
+    }
+}
+
+export class OpenClientLinkEvent extends MessageEvent implements IMessageEvent
+{
+    constructor(callBack: Function)
+    {
+        super(callBack, OpenClientLinkParser);
+    }
+
+    public getParser(): OpenClientLinkParser
+    {
+        return this.parser as OpenClientLinkParser;
+    }
+}
+
+// ──────────── SetTarget (header 6550, client→server) ────────────
+//
+// Emitted from the room InfoStand whenever the local player selects
+// another user's avatar sprite. The Arcturus-side handler fires
+// UserTargetSelectedEvent, which PixeltowerRP consumes to set the
+// target cache and push UpdateTargetStats back to this client.
+
+export class PixeltowerSetTargetComposer implements IMessageComposer<ConstructorParameters<typeof PixeltowerSetTargetComposer>>
+{
+    private _data: ConstructorParameters<typeof PixeltowerSetTargetComposer>;
+
+    constructor(targetHabboId: number)
+    {
+        this._data = [ targetHabboId ];
+    }
+
+    public getMessageArray()
+    {
+        return this._data;
+    }
+
+    public dispose(): void
+    {
+        return;
+    }
+}
+
+// ──────────── registration ────────────
+
+class PixeltowerMessages implements IMessageConfiguration
+{
+    public events: Map<number, Function> = new Map<number, Function>([
+        [ PIXELTOWER_PLAY_SOUND, NitroPlaySoundEvent ],
+        [ PIXELTOWER_UPDATE_PLAYER_STATS, UpdatePlayerStatsEvent ],
+        [ PIXELTOWER_UPDATE_TARGET_STATS, UpdateTargetStatsEvent ],
+        [ PIXELTOWER_OPEN_CLIENT_LINK, OpenClientLinkEvent ]
+    ]);
+    public composers: Map<number, Function> = new Map<number, Function>([
+        [ PIXELTOWER_SET_TARGET, PixeltowerSetTargetComposer ]
+    ]);
+}
+
+export function registerPixeltowerMessages(): void
+{
+    GetConnection()?.registerMessages(new PixeltowerMessages());
+}
diff --git a/src/assets/styles/bootstrap/_variables.scss b/src/assets/styles/bootstrap/_variables.scss
index eb668b3..4c44a69 100644
--- a/src/assets/styles/bootstrap/_variables.scss
+++ b/src/assets/styles/bootstrap/_variables.scss
@@ -70,8 +70,11 @@ $colors: (
 // scss-docs-end colors-map
 
 // scss-docs-start theme-color-variables
-$primary:       #1E7295 !default;
-$secondary:     #185D79 !default;
+// pixeltower: retune Bootstrap's teal $primary/$secondary to HUD slate so
+// every downstream Bootstrap consumer (.btn-primary, .bg-secondary,
+// .nitro-card-tabs, --bs-primary custom props, etc.) matches the HUD palette.
+$primary:       #3E4E6E !default;   // HUD panel fill (opaque of $pt-panel-fill)
+$secondary:     #2B3A54 !default;   // HUD chip-bottom ($pt-chip-bottom)
 $tertiary:      #2DABC2 !default;
 $quaternary:    #2B91A7 !default;
 $mirage:        #131e25 !default;
diff --git a/src/common/draggable-window/DraggableWindow.tsx b/src/common/draggable-window/DraggableWindow.tsx
index 286d51d..8fbd840 100644
--- a/src/common/draggable-window/DraggableWindow.tsx
+++ b/src/common/draggable-window/DraggableWindow.tsx
@@ -33,7 +33,11 @@ export const DraggableWindow: FC<DraggableWindowProps> = props =>
 
     const bringToTop = useCallback(() =>
     {
-        let zIndex = 400;
+        // Start above the pixeltower HUD tier (canvas z:1000, HUDs z:1100)
+        // so draggable windows — Mod Tools, Navigator, Catalog, etc. — are
+        // never occluded by StatsHUD / StatusHUD / TargetHUD / DrawerHUD /
+        // ToolbarHUD. Upstream default was 400, which sat below the canvas.
+        let zIndex = 1200;
 
         for(const existingWindow of CURRENT_WINDOWS)
         {
diff --git a/src/components/index.scss b/src/components/index.scss
index 8e1cd2b..197f628 100644
--- a/src/components/index.scss
+++ b/src/components/index.scss
@@ -17,10 +17,15 @@
 @import './navigator/NavigatorView';
 @import './nitropedia/NitropediaView';
 @import './notification-center/NotificationCenterView';
+@import './pixeltower/CommandsListView';
+@import './pixeltower/StatsHUD';
+@import './pixeltower/StatusHUD';
+@import './pixeltower/DrawerHUD';
+@import './pixeltower/ToolbarHUD';
+@import './pixeltower/app-overrides';
 @import './purse/PurseView';
 @import './right-side/RightSideView';
 @import './room/RoomView';
-@import './toolbar/ToolbarView';
 @import './user-profile/UserProfileVew';
 @import './wired/WiredView';
 @import './game-center/GameCenterView.scss';
diff --git a/src/components/main/MainView.tsx b/src/components/main/MainView.tsx
index da7003d..ca3029d 100644
--- a/src/components/main/MainView.tsx
+++ b/src/components/main/MainView.tsx
@@ -1,9 +1,8 @@
 import { HabboWebTools, ILinkEventTracker, RoomSessionEvent } from '@nitrots/nitro-renderer';
 import { FC, useEffect, useState } from 'react';
 import { AddEventLinkTracker, GetCommunication, RemoveLinkEventTracker } from '../../api';
-import { Base, TransitionAnimation, TransitionAnimationTypes } from '../../common';
+import { Base } from '../../common';
 import { useRoomSessionManagerEvent } from '../../hooks';
-import { AchievementsView } from '../achievements/AchievementsView';
 import { AvatarEditorView } from '../avatar-editor/AvatarEditorView';
 import { CameraWidgetView } from '../camera/CameraWidgetView';
 import { CampaignView } from '../campaign/CampaignView';
@@ -16,14 +15,19 @@ import { GroupsView } from '../groups/GroupsView';
 import { GuideToolView } from '../guide-tool/GuideToolView';
 import { HcCenterView } from '../hc-center/HcCenterView';
 import { HelpView } from '../help/HelpView';
-import { HotelView } from '../hotel-view/HotelView';
 import { InventoryView } from '../inventory/InventoryView';
 import { ModToolsView } from '../mod-tools/ModToolsView';
 import { NavigatorView } from '../navigator/NavigatorView';
+import { CommandsListView } from '../pixeltower/CommandsListView';
+import { DrawerHUD } from '../pixeltower/DrawerHUD';
+import { PixeltowerLinkListener } from '../pixeltower/PixeltowerLinkListener';
+import { StatsHUD } from '../pixeltower/StatsHUD';
+import { StatusHUD } from '../pixeltower/StatusHUD';
+import { TargetHUD } from '../pixeltower/TargetHUD';
+import { ToolbarHUD } from '../pixeltower/ToolbarHUD';
 import { NitropediaView } from '../nitropedia/NitropediaView';
 import { RightSideView } from '../right-side/RightSideView';
 import { RoomView } from '../room/RoomView';
-import { ToolbarView } from '../toolbar/ToolbarView';
 import { UserProfileView } from '../user-profile/UserProfileView';
 import { UserSettingsView } from '../user-settings/UserSettingsView';
 import { WiredView } from '../wired/WiredView';
@@ -81,16 +84,24 @@ export const MainView: FC<{}> = props =>
 
     return (
         <Base fit>
-            <TransitionAnimation type={ TransitionAnimationTypes.FADE_IN } inProp={ landingViewVisible } timeout={ 300 }>
-                <HotelView />
-            </TransitionAnimation>
-            <ToolbarView isInRoom={ !landingViewVisible } />
+            { /* HotelView intentionally removed — pixeltower pins users to a room on login
+                 (see PixeltowerRP.onUserLogin), so the hotel-view lobby is never exposed. */ }
+            { /* Upstream ToolbarView replaced by the pixeltower chat+phone HUD
+                 (.designs/client_ui.pen frame Y6gyQ). ToolbarView.tsx is left on
+                 disk for reference; it is no longer rendered. */ }
+            <PixeltowerLinkListener />
+            <ToolbarHUD />
+            <StatsHUD />
+            <StatusHUD />
+            <TargetHUD />
+            <DrawerHUD />
+            <CommandsListView />
             <ModToolsView />
             <RoomView />
             <ChatHistoryView />
             <WiredView />
             <AvatarEditorView />
-            <AchievementsView />
+            { /* AchievementsView unmounted — pixeltower disables achievements UI. */ }
             <NavigatorView />
             <InventoryView />
             <CatalogView />
diff --git a/src/components/notification-center/views/bubble-layouts/GetBubbleLayout.tsx b/src/components/notification-center/views/bubble-layouts/GetBubbleLayout.tsx
index b22a2f6..9d9ee64 100644
--- a/src/components/notification-center/views/bubble-layouts/GetBubbleLayout.tsx
+++ b/src/components/notification-center/views/bubble-layouts/GetBubbleLayout.tsx
@@ -1,5 +1,4 @@
 import { NotificationBubbleItem, NotificationBubbleType } from '../../../../api';
-import { NotificationClubGiftBubbleView } from './NotificationClubGiftBubbleView';
 import { NotificationDefaultBubbleView } from './NotificationDefaultBubbleView';
 
 export const GetBubbleLayout = (item: NotificationBubbleItem, onClose: () => void) =>
@@ -11,7 +10,9 @@ export const GetBubbleLayout = (item: NotificationBubbleItem, onClose: () => voi
     switch(item.notificationType)
     {
         case NotificationBubbleType.CLUBGIFT:
-            return <NotificationClubGiftBubbleView { ...props } />
+            // HC monthly gift prompt suppressed — pixeltower disables the
+            // nagging "choose your gift" pill that appears for HC members.
+            return null;
         default:
             return <NotificationDefaultBubbleView { ...props } />
     }
diff --git a/src/components/pixeltower/CommandsListView.scss b/src/components/pixeltower/CommandsListView.scss
new file mode 100644
index 0000000..023b264
--- /dev/null
+++ b/src/components/pixeltower/CommandsListView.scss
@@ -0,0 +1,187 @@
+@use './tokens' as *;
+
+// Commands List window — .designs/client_ui.pen frame I2LwC. The outer
+// shell is NitroCardView (theme="primary") so it uses Nitro's native
+// Habbo card chrome including the red close button; the custom red
+// close drawn in the .pen is intentionally disregarded for consistency
+// with every other Habbo dialog in the client.
+
+$pt-cmd-search-top:    #1A2436;
+$pt-cmd-search-bottom: #2B3A54;
+$pt-cmd-search-icon:   #7A95B8;
+$pt-cmd-search-hint:   #9FB3CC;
+
+$pt-cmd-tab-label:         #C4D1E0;
+$pt-cmd-tab-label-active:  #FFFFFF;
+$pt-cmd-tab-underglow:     #9AB4D3;
+
+$pt-cmd-table-bg:              #2B3A54;
+$pt-cmd-table-header-bg:       #1F2C40;
+$pt-cmd-table-header-fg:       #9FB3CC;
+$pt-cmd-table-row-desc:        #C4D1E0;
+$pt-cmd-table-row-param:       #7A95B8;
+$pt-cmd-table-row-placeholder: #FFD028;
+
+.pixeltower-commands-list {
+    width: 560px;
+    max-width: calc(100vw - 24px);
+    color: $pt-text;
+    font-family: $pt-font-family;
+
+    &__body {
+        display: flex;
+        flex-direction: column;
+        gap: 12px;
+        padding: 16px;
+        min-height: 0;
+    }
+
+    &__top-row {
+        display: flex;
+        flex-direction: column;
+        gap: 12px;
+    }
+
+    &__search {
+        display: flex;
+        align-items: center;
+        gap: 8px;
+        height: 36px;
+        padding: 0 12px;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 3px;
+        background: linear-gradient(180deg,
+            $pt-cmd-search-top 0%, $pt-cmd-search-top 50%,
+            $pt-cmd-search-bottom 50%, $pt-cmd-search-bottom 100%);
+        box-shadow: 0 -1px 0 $pt-cmd-tab-underglow;
+    }
+
+    &__search-icon {
+        width: 14px;
+        height: 14px;
+        color: $pt-cmd-search-icon;
+        flex-shrink: 0;
+    }
+
+    &__search-input {
+        flex: 1 1 auto;
+        min-width: 0;
+        background: transparent;
+        border: 0;
+        outline: none;
+        color: $pt-text;
+        font-family: inherit;
+        font-size: 14px;
+        font-weight: 700;
+
+        &::placeholder { color: $pt-cmd-search-hint; }
+    }
+
+    &__tabs {
+        display: flex;
+        height: 36px;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 3px;
+        overflow: hidden;
+        box-shadow: 0 1px 0 $pt-cmd-tab-underglow;
+    }
+
+    &__tab {
+        flex: 1 1 0;
+        background: linear-gradient(180deg,
+            $pt-chip-top 0%, $pt-chip-top 50%,
+            $pt-chip-bottom 50%, $pt-chip-bottom 100%);
+        border: 0;
+        padding: 0 16px;
+        color: $pt-cmd-tab-label;
+        font-family: inherit;
+        font-size: 14px;
+        font-weight: 700;
+        cursor: pointer;
+        transition: filter 120ms ease;
+
+        &:hover { filter: brightness(1.08); }
+
+        &--active {
+            background: linear-gradient(180deg,
+                $pt-chip-bottom 0%, $pt-chip-bottom 50%,
+                $pt-chip-top 50%, $pt-chip-top 100%);
+            color: $pt-cmd-tab-label-active;
+        }
+    }
+
+    &__tab-divider {
+        width: 2px;
+        background: $pt-panel-stroke;
+        flex-shrink: 0;
+    }
+
+    &__table {
+        display: flex;
+        flex-direction: column;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 3px;
+        background: $pt-cmd-table-bg;
+        overflow: hidden;
+        min-height: 0;
+    }
+
+    &__table-header {
+        display: flex;
+        gap: 12px;
+        padding: 8px 12px;
+        background: $pt-cmd-table-header-bg;
+        font-size: 12px;
+        font-weight: 700;
+        letter-spacing: 1px;
+        color: $pt-cmd-table-header-fg;
+        text-transform: uppercase;
+    }
+
+    &__table-body {
+        display: flex;
+        flex-direction: column;
+        max-height: 50vh;
+        overflow-y: auto;
+    }
+
+    &__row {
+        display: flex;
+        gap: 12px;
+        padding: 10px 12px;
+        align-items: center;
+        border-bottom: 1px solid $pt-panel-stroke;
+
+        &:last-child { border-bottom: 0; }
+    }
+
+    &__col-command {
+        width: 100px;
+        flex-shrink: 0;
+        font-size: 14px;
+        font-weight: 700;
+        color: $pt-text;
+    }
+
+    &__col-params {
+        width: 150px;
+        flex-shrink: 0;
+        font-size: 14px;
+        color: $pt-cmd-table-row-param;
+
+        &--placeholder { color: $pt-cmd-table-row-placeholder; }
+    }
+
+    &__col-description {
+        flex: 1 1 auto;
+        font-size: 14px;
+        color: $pt-cmd-table-row-desc;
+    }
+
+    &__empty {
+        padding: 24px 12px;
+        text-align: center;
+        color: $pt-cmd-table-row-desc;
+        font-size: 14px;
+    }
+}
diff --git a/src/components/pixeltower/CommandsListView.tsx b/src/components/pixeltower/CommandsListView.tsx
new file mode 100644
index 0000000..9f6fd9e
--- /dev/null
+++ b/src/components/pixeltower/CommandsListView.tsx
@@ -0,0 +1,180 @@
+import { ILinkEventTracker } from '@nitrots/nitro-renderer';
+import { FC, useEffect, useMemo, useState } from 'react';
+import { LuSearch } from 'react-icons/lu';
+import { AddEventLinkTracker, GetSessionDataManager, RemoveLinkEventTracker } from '../../api';
+import { NitroCardContentView, NitroCardHeaderView, NitroCardView } from '../../common';
+import { useEmployment, usePurse } from '../../hooks';
+
+// Reference window for every chat command the server + client expose.
+// Opens via `:commands` (see useChatInputWidget.ts).
+//
+// Tabs + their contents are gated per the viewer's status:
+//   - General → visible to everyone
+//   - Job     → only employed users (useEmployment — server signal TBD,
+//               stub currently returns isEmployed:false)
+//   - VIP     → only users with active HC (usePurse → totalClubDays > 0)
+//   - Staff   → only moderators (GetSessionDataManager().isModerator)
+//
+// Hidden tabs aren't rendered at all, so a regular player never sees
+// evidence of commands they can't run.
+
+type Category = 'general' | 'job' | 'vip' | 'staff';
+
+interface CommandDef {
+    name: string;
+    params?: string;
+    description: string;
+    category: Category;
+    /** When true, the params cell renders in the yellow placeholder style. */
+    paramsPlaceholder?: boolean;
+}
+
+const COMMANDS: CommandDef[] = [
+    { name: 'bal',          description: 'Check your bank balance',                  category: 'general' },
+    { name: 'target',       params: '%user%',           paramsPlaceholder: true, description: 'Target a player to view their stats',       category: 'general' },
+    { name: 'open-account', description: 'Open a corp bank account',                 category: 'general' },
+    { name: 'deposit',      params: '%amount%',         paramsPlaceholder: true, description: 'Deposit credits into your account',        category: 'general' },
+    { name: 'withdraw',     params: '%amount%',         paramsPlaceholder: true, description: 'Withdraw credits from your account',       category: 'general' },
+    { name: 'transfer',     params: '%user% %amount%',  paramsPlaceholder: true, description: 'Send credits to another user',             category: 'general' },
+    { name: 'stats',        description: 'Show your stats',                          category: 'general' },
+    { name: 'restore',      description: 'Restore your HP and energy',               category: 'general' },
+    { name: 'commands',     description: 'Open this reference window',               category: 'general' },
+    { name: 'startwork',    description: 'Start a shift at your corp',               category: 'job' },
+    { name: 'stopwork',     description: 'End your current shift',                   category: 'job' },
+    { name: 'give',         params: '%user% %amount%',  paramsPlaceholder: true, description: 'Grant credits to a user',                  category: 'staff' },
+    { name: 'award',        params: '%user% %amount%',  paramsPlaceholder: true, description: 'Award currency to a user',                 category: 'staff' },
+    { name: 'hire',         params: '%user% %corp%',    paramsPlaceholder: true, description: 'Hire a user into a corp',                  category: 'staff' },
+    { name: 'fire',         params: '%user%',           paramsPlaceholder: true, description: 'Remove a user from their corp',            category: 'staff' },
+    { name: 'promote',      params: '%user%',           paramsPlaceholder: true, description: "Promote a user's corp rank",               category: 'staff' },
+    { name: 'roomsettings', description: 'Open room settings for the current room',  category: 'staff' },
+    { name: 'floorplan',    description: 'Open the floor plan editor',               category: 'staff' }
+];
+
+const TAB_LABEL: Record<Category, string> = {
+    general: 'General',
+    job:     'Job',
+    vip:     'VIP',
+    staff:   'Staff'
+};
+
+export const CommandsListView: FC<{}> = () =>
+{
+    const [ isVisible, setIsVisible ] = useState(false);
+    const [ activeTab, setActiveTab ] = useState<Category>('general');
+    const [ query, setQuery ] = useState<string>('');
+
+    const { purse = null } = usePurse();
+    const { isEmployed } = useEmployment();
+    const isModerator = GetSessionDataManager().isModerator;
+    const totalClubDays = purse ? (purse.clubPeriods * 31) + purse.clubDays : 0;
+    const isVip = totalClubDays > 0;
+
+    const visibleTabs = useMemo<Category[]>(() =>
+    {
+        const tabs: Category[] = [ 'general' ];
+        if(isEmployed) tabs.push('job');
+        if(isVip) tabs.push('vip');
+        if(isModerator) tabs.push('staff');
+        return tabs;
+    }, [ isEmployed, isVip, isModerator ]);
+
+    // If the active tab becomes hidden (e.g. HC expired while the
+    // window was open), fall back to General so the body doesn't render
+    // a tab the user no longer has access to.
+    useEffect(() =>
+    {
+        if(!visibleTabs.includes(activeTab)) setActiveTab('general');
+    }, [ visibleTabs, activeTab ]);
+
+    useEffect(() =>
+    {
+        const linkTracker: ILinkEventTracker = {
+            linkReceived: (url: string) =>
+            {
+                const parts = url.split('/');
+                if(parts.length < 2) return;
+                switch(parts[1])
+                {
+                    case 'show':   setIsVisible(true); return;
+                    case 'hide':   setIsVisible(false); return;
+                    case 'toggle': setIsVisible(v => !v); return;
+                }
+            },
+            eventUrlPrefix: 'commands-list/'
+        };
+        AddEventLinkTracker(linkTracker);
+        return () => RemoveLinkEventTracker(linkTracker);
+    }, []);
+
+    const filtered = useMemo(() =>
+    {
+        const q = query.trim().toLowerCase();
+        return COMMANDS.filter(c =>
+        {
+            if(c.category !== activeTab) return false;
+            if(!q) return true;
+            return c.name.toLowerCase().includes(q) || c.description.toLowerCase().includes(q);
+        });
+    }, [ activeTab, query ]);
+
+    if(!isVisible) return null;
+
+    return (
+        <NitroCardView uniqueKey="pixeltower-commands" theme="primary" classNames={ [ 'pixeltower-commands-list' ] }>
+            <NitroCardHeaderView headerText="Commands List" onCloseClick={ () => setIsVisible(false) } />
+            <NitroCardContentView classNames={ [ 'pixeltower-commands-list__body' ] } overflow="hidden">
+                <div className="pixeltower-commands-list__top-row">
+                    <label className="pixeltower-commands-list__search">
+                        <LuSearch className="pixeltower-commands-list__search-icon" />
+                        <input
+                            type="text"
+                            className="pixeltower-commands-list__search-input"
+                            placeholder="Search commands…"
+                            value={ query }
+                            onChange={ e => setQuery(e.target.value) } />
+                    </label>
+                    <div className="pixeltower-commands-list__tabs">
+                        { visibleTabs.flatMap((tab, i) =>
+                        {
+                            const btn = (
+                                <button
+                                    key={ tab }
+                                    type="button"
+                                    className={ 'pixeltower-commands-list__tab' + (tab === activeTab ? ' pixeltower-commands-list__tab--active' : '') }
+                                    onClick={ () => setActiveTab(tab) }>
+                                    { TAB_LABEL[tab] }
+                                </button>
+                            );
+                            return i === 0
+                                ? [ btn ]
+                                : [ <div key={ `${ tab }-div` } className="pixeltower-commands-list__tab-divider" />, btn ];
+                        }) }
+                    </div>
+                </div>
+                <div className="pixeltower-commands-list__table">
+                    <div className="pixeltower-commands-list__table-header">
+                        <span className="pixeltower-commands-list__col-command">Command</span>
+                        <span className="pixeltower-commands-list__col-params">Parameters</span>
+                        <span className="pixeltower-commands-list__col-description">Description</span>
+                    </div>
+                    <div className="pixeltower-commands-list__table-body">
+                        { filtered.length === 0
+                            ? <div className="pixeltower-commands-list__empty">
+                                { activeTab === 'vip' ? 'No VIP commands yet.' : 'No commands match your search.' }
+                            </div>
+                            : filtered.map(cmd => (
+                                <div key={ cmd.name } className="pixeltower-commands-list__row">
+                                    <span className="pixeltower-commands-list__col-command">{ cmd.name }</span>
+                                    <span className={ 'pixeltower-commands-list__col-params' + (cmd.paramsPlaceholder ? ' pixeltower-commands-list__col-params--placeholder' : '') }>
+                                        { cmd.params ?? 'N/A' }
+                                    </span>
+                                    <span className="pixeltower-commands-list__col-description">{ cmd.description }</span>
+                                </div>
+                            ))
+                        }
+                    </div>
+                </div>
+            </NitroCardContentView>
+        </NitroCardView>
+    );
+}
diff --git a/src/components/pixeltower/DrawerHUD.scss b/src/components/pixeltower/DrawerHUD.scss
new file mode 100644
index 0000000..cba53ef
--- /dev/null
+++ b/src/components/pixeltower/DrawerHUD.scss
@@ -0,0 +1,99 @@
+@use './tokens' as *;
+
+// Left-edge collapsible drawer — matches .designs/client_ui.pen frame
+// tW1BU. Scaled down from the design's 56x56 buttons / 22x44 handle to
+// 40x40 / 17x34 for in-game rhythm alongside the other HUDs.
+// Anchored flush to the left edge via cornerRadius [0, n, n, 0].
+
+.pixeltower-drawer-hud {
+    position: absolute;
+    top: 50%;
+    left: 0;
+    transform: translateY(-50%);
+    // Above Nitro's global `canvas { z-index: 1000 }`.
+    z-index: 1100;
+
+    display: flex;
+    flex-direction: row;
+    align-items: center;
+    pointer-events: none;                           // wrapper passthrough; children opt back in
+
+    &__panel {
+        @include pt-panel;
+        border-top-left-radius: 0;                  // flush against viewport left edge
+        border-bottom-left-radius: 0;
+
+        display: flex;
+        flex-direction: column;
+        gap: 6px;
+        padding: 6px;
+
+        max-width: 0;
+        padding-left: 0;
+        padding-right: 0;
+        border-left-width: 0;
+        overflow: hidden;
+        opacity: 0;
+        transition:
+            max-width 220ms ease-out,
+            padding 220ms ease-out,
+            border-left-width 220ms ease-out,
+            opacity 180ms ease-out;
+        pointer-events: auto;
+    }
+
+    &--open &__panel {
+        max-width: 58px;                            // 40 button + 2x6 padding + stroke
+        padding-left: 6px;
+        padding-right: 6px;
+        border-left-width: 0;                       // keep flush left
+        opacity: 1;
+    }
+
+    &__button {
+        @include pt-chip;
+        width: 40px;
+        height: 40px;
+        padding: 0;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        color: $pt-text;
+        cursor: pointer;
+        font-family: inherit;
+        transition: filter 150ms ease;
+
+        svg { width: 17px; height: 17px; }
+
+        &:hover { filter: brightness(1.12); }
+    }
+
+    &__handle {
+        // Panel treatment but drop-shadow only — no periwinkle rim-light
+        // (per design: the handle reads slightly flatter than the drawer).
+        background: $pt-panel-fill;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 5px;
+        border-top-left-radius: 0;
+        border-bottom-left-radius: 0;
+        border-left-width: 0;
+        backdrop-filter: blur($pt-panel-blur);
+        -webkit-backdrop-filter: blur($pt-panel-blur);
+        box-shadow: 0 2px 0 $pt-panel-shadow-drop;
+
+        width: 17px;
+        height: 34px;
+        padding: 0;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        color: $pt-text;
+        cursor: pointer;
+        pointer-events: auto;
+        transition: filter 150ms ease;
+
+        svg { width: 12px; height: 12px; }
+
+        &:hover { filter: brightness(1.12); }
+    }
+}
diff --git a/src/components/pixeltower/DrawerHUD.tsx b/src/components/pixeltower/DrawerHUD.tsx
new file mode 100644
index 0000000..7a6d30c
--- /dev/null
+++ b/src/components/pixeltower/DrawerHUD.tsx
@@ -0,0 +1,54 @@
+import { FC, useState } from 'react';
+import {
+    LuBackpack, LuBuilding2, LuChevronLeft, LuChevronRight,
+    LuKeyboard, LuScroll, LuUsers
+} from 'react-icons/lu';
+
+// Left-edge side drawer for RP-specific actions. Matches
+// .designs/client_ui.pen frame tW1BU: a narrow pull-tab handle that
+// stays flush against the viewport's left edge, and an expandable
+// panel of icon-only action buttons that slides out on click.
+//
+// Click handlers are deliberately no-ops for now — the RP backends
+// (Gangs, Corps, Macros, Wanted) don't yet exist server-side. Swap
+// in CreateLinkEvent(...) calls per-button as those ship.
+
+type DrawerButton = { key: string; label: string; Icon: typeof LuBackpack };
+
+const BUTTONS: DrawerButton[] = [
+    { key: 'inventory', label: 'Inventory', Icon: LuBackpack },
+    { key: 'macros',    label: 'Macros',    Icon: LuKeyboard },
+    { key: 'gangs',     label: 'Gangs',     Icon: LuUsers },
+    { key: 'corps',     label: 'Corps',     Icon: LuBuilding2 },
+    { key: 'wanted',    label: 'Wanted',    Icon: LuScroll },
+];
+
+export const DrawerHUD: FC<{}> = () =>
+{
+    const [ open, setOpen ] = useState(false);
+    const Chev = open ? LuChevronLeft : LuChevronRight;
+
+    return (
+        <div className={ 'pixeltower-drawer-hud' + (open ? ' pixeltower-drawer-hud--open' : '') }>
+            <div className="pixeltower-drawer-hud__panel">
+                { BUTTONS.map(({ key, label, Icon }) => (
+                    <button
+                        key={ key }
+                        type="button"
+                        className="pixeltower-drawer-hud__button"
+                        title={ label }
+                        onClick={ () => { /* placeholder — wire RP actions later */ } }>
+                        <Icon />
+                    </button>
+                )) }
+            </div>
+            <button
+                type="button"
+                className="pixeltower-drawer-hud__handle"
+                title={ open ? 'Collapse' : 'Expand' }
+                onClick={ () => setOpen(o => !o) }>
+                <Chev />
+            </button>
+        </div>
+    );
+}
diff --git a/src/components/pixeltower/PixeltowerLinkListener.tsx b/src/components/pixeltower/PixeltowerLinkListener.tsx
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/components/pixeltower/PixeltowerLinkListener.tsx
@@ -0,0 +1,38 @@
+import { FC } from 'react';
+import { CreateLinkEvent } from '../../api';
+import { OpenClientLinkEvent } from '../../api/pixeltower/PixeltowerSoundMessages';
+import { useMessageEvent } from '../../hooks';
+
+// Allow-list of Nitro link prefixes the rp_functional dispatcher (server
+// header 6503, OpenClientLinkComposer) is permitted to fire via
+// CreateLinkEvent. Defense-in-depth: the source is our own server, but a
+// misconfigured DB row should not be able to open arbitrary surfaces
+// (mod-tools, room-info, etc.). Add new prefixes only after pairing them
+// with a server-side action_type case in FunctionalActionDispatcher.
+const ALLOWED_LINK_PREFIXES = [
+    'avatar-editor/',
+    'navigator/',
+    'catalog/',
+    'wardrobe/',
+    'floor-editor/'
+];
+
+// Headless component — registers a useMessageEvent on mount and renders
+// nothing. Mounted once at the top of MainView so the listener lives for
+// the entire authenticated session.
+export const PixeltowerLinkListener: FC<{}> = () =>
+{
+    useMessageEvent<OpenClientLinkEvent>(OpenClientLinkEvent, event =>
+    {
+        const uri = event.getParser().uri;
+        if(!uri) return;
+        if(!ALLOWED_LINK_PREFIXES.some(prefix => uri.startsWith(prefix)))
+        {
+            console.warn('[PixeltowerRP] OpenClientLink rejected, uri=', uri);
+            return;
+        }
+        CreateLinkEvent(uri);
+    });
+
+    return null;
+};
diff --git a/src/components/pixeltower/StatsHUD.scss b/src/components/pixeltower/StatsHUD.scss
new file mode 100644
index 0000000..0103fb3
--- /dev/null
+++ b/src/components/pixeltower/StatsHUD.scss
@@ -0,0 +1,235 @@
+@use './tokens' as *;
+
+// Player HUD + Target HUD styles.
+// Visual language derived from .designs/client_ui.pen frames HzOI4 (Player)
+// and 1hvgH (Target), scaled from the design's 480px width down to 280px
+// in-game (~58%). Chunky 2D-bevel aesthetic: translucent slate-blue panel,
+// 2px near-navy outline, hard 50/50 gradient bars, tinted 1px underglows.
+
+.pixeltower-stats-hud {
+    position: absolute;
+    top: 12px;
+    left: 12px;
+    // Above Nitro's global `canvas { z-index: 1000 }`.
+    z-index: 1100;
+
+    // Container is transparent per .designs/client_ui.pen frames HzOI4
+    // (Player) and 1hvgH (Target) — no background, border, or blur. The
+    // inner avatar frame and bars carry their own chrome.
+    background: transparent;
+    width: 280px;
+    padding: 6px;
+
+    color: $pt-text;
+    font-family: $pt-font-family;
+    pointer-events: none;
+    user-select: none;
+
+    // Wanted-flag gradient sprite. The <svg> is 0×0 but absolute so
+    // it doesn't reserve an inline line-box at the top of the HUD
+    // (which would otherwise shift the avatar/bars row down ~20px).
+    &__defs {
+        position: absolute;
+        width: 0;
+        height: 0;
+        overflow: hidden;
+        pointer-events: none;
+    }
+
+    &__row {
+        display: flex;
+        flex-direction: row;
+        align-items: stretch;
+        gap: 6px;
+    }
+
+    // ──────────── avatar frame ────────────
+
+    // Avatar wrap — .pen frame HzOI4 > wKu20 specifies a flat dark-navy
+    // fill + matching 2px outside stroke. Earlier gold frame (VIP-like
+    // yellow) was dropped in the updated design.
+    &__avatar {
+        flex-shrink: 0;
+        width: 72px;
+        height: 72px;
+        padding: 2px;
+        border-radius: 5px;
+        background: $pt-panel-stroke;
+        border: 2px solid $pt-panel-stroke;
+    }
+
+    &__avatar-inner {
+        position: relative;
+        width: 100%;
+        height: 100%;
+        border-radius: 3px;
+        background: linear-gradient(180deg,
+            $pt-avatar-inner-top 0%, $pt-avatar-inner-top 50%,
+            $pt-avatar-inner-bottom 50%, $pt-avatar-inner-bottom 100%);
+        overflow: hidden;
+        display: flex;
+        flex-direction: column;
+        justify-content: flex-end;
+
+        .avatar-image {
+            position: absolute;
+            top: -30px;
+            left: 50%;
+            transform: translateX(-50%);
+            width: 64px;
+            height: 110px;
+            background-position: center top;
+            background-repeat: no-repeat;
+            image-rendering: pixelated;
+        }
+    }
+
+    &__name {
+        position: relative;
+        padding: 3px 4px;
+        background: $pt-username-badge;
+        text-align: center;
+        font-size: 10px;
+        font-weight: 900;
+        line-height: 1.1;
+        overflow: hidden;
+        text-overflow: ellipsis;
+        white-space: nowrap;
+    }
+
+    // ──────────── bars column ────────────
+
+    &__bars {
+        display: flex;
+        flex-direction: column;
+        gap: 4px;
+        flex: 1;
+        min-width: 0;
+        justify-content: center;
+    }
+
+    &__bar {
+        position: relative;
+        height: 22px;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 5px;
+        overflow: hidden;
+        box-sizing: border-box;
+    }
+
+    &__bar-track {
+        position: absolute;
+        inset: 0;
+    }
+
+    &__bar--hp {
+        border-color: $pt-hp-stroke;
+        box-shadow: 0 1px 0 $pt-hp-glow;
+
+        .pixeltower-stats-hud__bar-track {
+            background: linear-gradient(180deg,
+                $pt-hp-track-top 0%, $pt-hp-track-top 50%,
+                $pt-hp-track-bottom 50%, $pt-hp-track-bottom 100%);
+        }
+    }
+
+    &__bar--energy {
+        border-color: $pt-energy-stroke;
+        box-shadow: 0 1px 0 $pt-energy-glow;
+
+        .pixeltower-stats-hud__bar-track {
+            background: linear-gradient(180deg,
+                $pt-energy-track-top 0%, $pt-energy-track-top 50%,
+                $pt-energy-track-bottom 50%, $pt-energy-track-bottom 100%);
+        }
+    }
+
+    &__bar-fill {
+        position: absolute;
+        top: 0;
+        left: 0;
+        bottom: 0;
+        transition: width 300ms ease-out;
+        will-change: width;
+
+        &--hp {
+            background: linear-gradient(180deg,
+                $pt-hp-top 0%, $pt-hp-top 50%,
+                $pt-hp-bottom 50%, $pt-hp-bottom 100%);
+        }
+        &--energy {
+            background: linear-gradient(180deg,
+                $pt-energy-top 0%, $pt-energy-top 50%,
+                $pt-energy-bottom 50%, $pt-energy-bottom 100%);
+        }
+    }
+
+    &__bar-content {
+        position: absolute;
+        inset: 0;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        gap: 6px;
+        padding: 0 8px;
+        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.7);
+    }
+
+    &__bar-icon {
+        width: 12px;
+        height: 12px;
+        color: #fff;
+        flex-shrink: 0;
+    }
+
+    &__bar-label {
+        font-size: 11px;
+        font-weight: 900;
+        font-variant-numeric: tabular-nums;
+        flex: 1;
+        text-align: center;
+    }
+
+    // ──────────── wanted flags (5 slots) ────────────
+
+    &__wanted {
+        display: flex;
+        flex-direction: row;
+        gap: 3px;
+        padding: 0 2px;
+    }
+
+    // Custom SVG crest (see WantedFlag in StatsHUD.tsx). Fill comes
+    // from the shared gradient defs; this rule just controls render
+    // size + a subtle halo drop-shadow for contrast against rooms.
+    &__wanted-flag {
+        width: 14px;
+        height: 14px;
+        flex-shrink: 0;
+        display: block;
+        overflow: visible;
+        filter: drop-shadow(0 0 1px $pt-panel-stroke);
+    }
+
+    &__wanted-flag--active {
+        filter: drop-shadow(0 0 2px rgba(255, 244, 176, 0.65));
+    }
+
+    // ──────────── Target HUD variant (mirrored layout) ────────────
+
+    &--target {
+        left: 300px;
+
+        .pixeltower-stats-hud__row { flex-direction: row-reverse; }
+        .pixeltower-stats-hud__bar-content { flex-direction: row-reverse; }
+        .pixeltower-stats-hud__name {
+            background: $pt-username-badge-target;
+            padding: 4px 5px;                      // design [6,8] scaled (Player stays [4,8])
+        }
+        // Target HUD has no wanted-flag row (crime level is a self-only
+        // concept). Reserve the space that row would otherwise occupy —
+        // one flag-height (14px) plus the column's inter-item gap (4px) —
+        // so the HP/energy bars center-align identically to the Player HUD.
+        .pixeltower-stats-hud__bars { padding-bottom: 18px; }
+    }
+}
diff --git a/src/components/pixeltower/StatsHUD.tsx b/src/components/pixeltower/StatsHUD.tsx
new file mode 100644
index 0000000..97e3ffc
--- /dev/null
+++ b/src/components/pixeltower/StatsHUD.tsx
@@ -0,0 +1,111 @@
+import { FC } from 'react';
+import { LuHeart, LuZap } from 'react-icons/lu';
+import { GetSessionDataManager } from '../../api';
+import { LayoutAvatarImageView } from '../../common';
+import { useSessionInfo, useStats } from '../../hooks';
+
+const WANTED_SLOT_COUNT = 5;
+
+// Crime-level indicator — custom crest shape from .designs/client_ui.pen
+// frame HzOI4 > VFQ5Z (w1..w5). Authored at 32×32, scaled to 14×14
+// in-game via SVG viewBox. vectorEffect keeps the 2px stroke crisp
+// at the render scale. Fills reference the shared gradient sprite
+// defined once at the top of StatsHUD.
+const WANTED_FLAG_PATH = 'M16 32l-14-14q-2-2-2-5l0-9q0-4 4-4l24 0q4 0 4 4l0 9q0 3-2 5z';
+
+const WantedFlag: FC<{ active?: boolean }> = ({ active = false }) => (
+    <svg
+        viewBox="0 0 32 32"
+        className={ 'pixeltower-stats-hud__wanted-flag' + (active ? ' pixeltower-stats-hud__wanted-flag--active' : '') }
+        aria-hidden="true">
+        <path
+            d={ WANTED_FLAG_PATH }
+            fill={ `url(#pt-wanted-flag-${ active ? 'active' : 'inactive' })` }
+            stroke="#0A1424"
+            strokeWidth={ 2 }
+            vectorEffect="non-scaling-stroke" />
+    </svg>
+);
+
+/**
+ * Persistent top-left Player HUD. Matches the canonical design at
+ * .designs/client_ui.pen frame HzOI4, at a scaled-down size for the
+ * in-game context. Data comes live from UpdatePlayerStatsEvent via the
+ * useStats hook — HP/energy bars deplete in real time. Wanted-slot
+ * indicators are always visible (grey when unwanted).
+ */
+export const StatsHUD: FC<{}> = () =>
+{
+    const { stats = null } = useStats();
+    const { userFigure = null } = useSessionInfo();
+
+    if(!stats) return null;
+
+    const userName = GetSessionDataManager()?.userName ?? '';
+    const hpPct = stats.maxHp > 0 ? Math.max(0, Math.min(100, (stats.hp / stats.maxHp) * 100)) : 0;
+    const energyPct = stats.maxEnergy > 0 ? Math.max(0, Math.min(100, (stats.energy / stats.maxEnergy) * 100)) : 0;
+
+    // Wanted / crime-stars: always render the five slots. The server
+    // doesn't yet emit a wanted level (Tier 4 bounty work) — slots stay
+    // inactive for now.
+    const wantedLevel = 0;
+
+    return (
+        <div className="pixeltower-stats-hud">
+            { /* Zero-sized SVG sprite carrying the hard-bevel gradient
+                 defs for the wanted flags. Declared once at the root so
+                 all five flag instances share the same two gradients. */ }
+            <svg
+                width={ 0 }
+                height={ 0 }
+                className="pixeltower-stats-hud__defs"
+                aria-hidden="true">
+                <defs>
+                    <linearGradient id="pt-wanted-flag-inactive" x1="0" y1="0" x2="0" y2="1">
+                        <stop offset="0%" stopColor="#4A5568" />
+                        <stop offset="50%" stopColor="#4A5568" />
+                        <stop offset="50%" stopColor="#1F2833" />
+                        <stop offset="100%" stopColor="#1F2833" />
+                    </linearGradient>
+                    <linearGradient id="pt-wanted-flag-active" x1="0" y1="0" x2="0" y2="1">
+                        <stop offset="0%" stopColor="#FFE066" />
+                        <stop offset="50%" stopColor="#FFE066" />
+                        <stop offset="50%" stopColor="#D99A00" />
+                        <stop offset="100%" stopColor="#D99A00" />
+                    </linearGradient>
+                </defs>
+            </svg>
+            <div className="pixeltower-stats-hud__row">
+                <div className="pixeltower-stats-hud__avatar">
+                    <div className="pixeltower-stats-hud__avatar-inner">
+                        <LayoutAvatarImageView figure={ userFigure } direction={ 2 } />
+                        <div className="pixeltower-stats-hud__name">{ userName }</div>
+                    </div>
+                </div>
+                <div className="pixeltower-stats-hud__bars">
+                    <div className="pixeltower-stats-hud__bar pixeltower-stats-hud__bar--hp">
+                        <div className="pixeltower-stats-hud__bar-track" />
+                        <div className="pixeltower-stats-hud__bar-fill pixeltower-stats-hud__bar-fill--hp" style={ { width: `${ hpPct }%` } } />
+                        <div className="pixeltower-stats-hud__bar-content">
+                            <LuHeart className="pixeltower-stats-hud__bar-icon" />
+                            <span className="pixeltower-stats-hud__bar-label">{ stats.hp }/{ stats.maxHp }</span>
+                        </div>
+                    </div>
+                    <div className="pixeltower-stats-hud__bar pixeltower-stats-hud__bar--energy">
+                        <div className="pixeltower-stats-hud__bar-track" />
+                        <div className="pixeltower-stats-hud__bar-fill pixeltower-stats-hud__bar-fill--energy" style={ { width: `${ energyPct }%` } } />
+                        <div className="pixeltower-stats-hud__bar-content">
+                            <LuZap className="pixeltower-stats-hud__bar-icon" />
+                            <span className="pixeltower-stats-hud__bar-label">{ stats.energy }/{ stats.maxEnergy }</span>
+                        </div>
+                    </div>
+                    <div className="pixeltower-stats-hud__wanted">
+                        { Array.from({ length: WANTED_SLOT_COUNT }).map((_, i) => (
+                            <WantedFlag key={ i } active={ i < wantedLevel } />
+                        )) }
+                    </div>
+                </div>
+            </div>
+        </div>
+    );
+}
diff --git a/src/components/pixeltower/StatusHUD.scss b/src/components/pixeltower/StatusHUD.scss
new file mode 100644
index 0000000..893daa9
--- /dev/null
+++ b/src/components/pixeltower/StatusHUD.scss
@@ -0,0 +1,113 @@
+@use './tokens' as *;
+
+// Top-right Status HUD — matches .designs/client_ui.pen frame KVhc5.
+// The HUD absorbs the former InfoPillsHUD (room / time / online pills)
+// into its top rows; chat-history + user-settings buttons moved to
+// the ToolbarHUD. The outer container is transparent per the updated
+// design — each pill/row carries its own chrome.
+
+.pixeltower-status-hud {
+    position: absolute;
+    top: 12px;
+    right: 12px;
+    // Above Nitro's global `canvas { z-index: 1000 }`.
+    z-index: 1100;
+
+    // Full-scale layout reads as too heavy in the top-right corner;
+    // scale the whole HUD down 25% from its anchor so it sits as an
+    // accent next to the StatsHUD rather than dominating the screen.
+    transform: scale(0.75);
+    transform-origin: top right;
+
+    display: flex;
+    flex-direction: column;
+    align-items: flex-end;
+    width: fit-content;
+    gap: 8px;
+
+    background: transparent;
+    color: $pt-text;
+    font-family: $pt-font-family;
+    pointer-events: auto;
+    user-select: none;
+
+    // ──────────── info pills (room / time / online) ────────────
+
+    &__room-pill {
+        @include pt-panel;
+        border-radius: 3px;
+        padding: 6px 12px;
+        gap: 6px;
+        display: flex;
+        flex-direction: row;
+        align-items: center;
+        align-self: stretch;
+        white-space: nowrap;
+    }
+
+    &__info-row {
+        display: flex;
+        flex-direction: row;
+        gap: 8px;
+    }
+
+    &__info-pill {
+        @include pt-panel;
+        border-radius: 3px;
+        padding: 6px 12px;
+        gap: 6px;
+        display: flex;
+        flex-direction: row;
+        align-items: center;
+        white-space: nowrap;
+    }
+
+    &__pill-icon {
+        width: 16px;
+        height: 16px;
+        flex-shrink: 0;
+        color: $pt-text;
+    }
+
+    &__pill-text {
+        font-size: 15px;
+        font-weight: 700;
+        line-height: 1;
+        font-variant-numeric: tabular-nums;
+    }
+
+    // ──────────── currency + VIP rows ────────────
+    // Coin, diamond, and VIP rows share the same pt-panel shell as the
+    // info pills above — the only VIP-specific treatment is a yellow
+    // crown icon to flag subscription status.
+
+    &__row {
+        @include pt-panel;
+        border-radius: 3px;
+        display: flex;
+        flex-direction: row;
+        align-items: center;
+        gap: 6px;
+        padding: 6px 12px;
+        box-sizing: border-box;
+        align-self: stretch;
+    }
+
+    &__row-icon {
+        width: 16px;
+        height: 16px;
+        flex-shrink: 0;
+        color: $pt-text;
+    }
+
+    &__row-icon--vip {
+        color: #FFD028;
+    }
+
+    &__row-value {
+        font-size: 15px;
+        font-weight: 700;
+        line-height: 1;
+        font-variant-numeric: tabular-nums;
+    }
+}
diff --git a/src/components/pixeltower/StatusHUD.tsx b/src/components/pixeltower/StatusHUD.tsx
new file mode 100644
index 0000000..833dc80
--- /dev/null
+++ b/src/components/pixeltower/StatusHUD.tsx
@@ -0,0 +1,112 @@
+import { GetGuestRoomResultEvent } from '@nitrots/nitro-renderer';
+import { FC, useEffect, useState } from 'react';
+import { LuAlarmClock, LuCrown, LuGem, LuMapPin, LuUsers, LuWallet } from 'react-icons/lu';
+import { useMessageEvent, usePurse, useRoom } from '../../hooks';
+
+const DIAMOND_CURRENCY_TYPE = 5;
+
+const formatTime = (date: Date): string =>
+    date.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });
+
+// Top-right Status HUD — matches .designs/client_ui.pen frame KVhc5.
+//
+// Rows, top → bottom:
+//   1. Room pill       (LuMapPin + current room name)
+//   2. Info row        (time pill, online-users pill)
+//   3. Coin row        (LuWallet + credits, slate chip)
+//   4. Diamond row     (LuGem + activity points, slate chip)
+//   5. VIP row         (LuCrown + remaining HC days, green chip)
+//
+// Data sources:
+//   - purse   → usePurse (credits, activity points, HC days)
+//   - room    → GetGuestRoomResultEvent (same pattern as RoomToolsWidgetView)
+//   - time    → local clock, ticked every 30s so a minute rollover is
+//               never more than 30s stale
+//   - online  → AtomCMS /api/online-count, polled every 60s (nginx
+//               routes /api to Laravel)
+
+export const StatusHUD: FC<{}> = () =>
+{
+    const { purse = null } = usePurse();
+    const { roomSession = null } = useRoom();
+    const [ roomName, setRoomName ] = useState<string>('');
+    const [ time, setTime ] = useState<string>(() => formatTime(new Date()));
+    const [ onlineCount, setOnlineCount ] = useState<number | null>(null);
+
+    useMessageEvent<GetGuestRoomResultEvent>(GetGuestRoomResultEvent, event =>
+    {
+        const parser = event.getParser();
+
+        if(!parser.roomEnter) return;
+        if(roomSession && parser.data.roomId !== roomSession.roomId) return;
+        if(roomName !== parser.data.roomName) setRoomName(parser.data.roomName);
+    });
+
+    useEffect(() =>
+    {
+        const id = window.setInterval(() => setTime(formatTime(new Date())), 30_000);
+        return () => window.clearInterval(id);
+    }, []);
+
+    useEffect(() =>
+    {
+        let cancelled = false;
+
+        const fetchCount = async () =>
+        {
+            try
+            {
+                const res = await fetch('/api/online-count', { headers: { Accept: 'application/json' } });
+                if(!res.ok) return;
+                const json = await res.json();
+                // Laravel's JsonResource wraps the payload in a `data` envelope.
+                if(!cancelled && typeof json?.data?.onlineCount === 'number') setOnlineCount(json.data.onlineCount);
+            }
+            catch
+            {
+                // Network blip — keep last-known value.
+            }
+        };
+
+        fetchCount();
+        const id = window.setInterval(fetchCount, 60_000);
+        return () => { cancelled = true; window.clearInterval(id); };
+    }, []);
+
+    if(!purse) return null;
+
+    const totalClubDays = (purse.clubPeriods * 31) + purse.clubDays;
+    const coins = Math.floor(purse.credits);
+    const diamonds = purse.activityPoints?.get(DIAMOND_CURRENCY_TYPE) ?? 0;
+
+    return (
+        <div className="pixeltower-status-hud">
+            <div className="pixeltower-status-hud__room-pill">
+                <LuMapPin className="pixeltower-status-hud__pill-icon" />
+                <span className="pixeltower-status-hud__pill-text">{ roomName || '—' }</span>
+            </div>
+            <div className="pixeltower-status-hud__info-row">
+                <div className="pixeltower-status-hud__info-pill">
+                    <LuAlarmClock className="pixeltower-status-hud__pill-icon" />
+                    <span className="pixeltower-status-hud__pill-text">{ time }</span>
+                </div>
+                <div className="pixeltower-status-hud__info-pill">
+                    <LuUsers className="pixeltower-status-hud__pill-icon" />
+                    <span className="pixeltower-status-hud__pill-text">{ onlineCount !== null ? `${ onlineCount } online` : '— online' }</span>
+                </div>
+            </div>
+            <div className="pixeltower-status-hud__row">
+                <LuWallet className="pixeltower-status-hud__row-icon" />
+                <span className="pixeltower-status-hud__row-value">{ coins }</span>
+            </div>
+            <div className="pixeltower-status-hud__row">
+                <LuGem className="pixeltower-status-hud__row-icon" />
+                <span className="pixeltower-status-hud__row-value">{ diamonds }</span>
+            </div>
+            <div className="pixeltower-status-hud__row">
+                <LuCrown className="pixeltower-status-hud__row-icon pixeltower-status-hud__row-icon--vip" />
+                <span className="pixeltower-status-hud__row-value">VIP · { totalClubDays } d.</span>
+            </div>
+        </div>
+    );
+}
diff --git a/src/components/pixeltower/TargetHUD.tsx b/src/components/pixeltower/TargetHUD.tsx
new file mode 100644
index 0000000..5acbbdb
--- /dev/null
+++ b/src/components/pixeltower/TargetHUD.tsx
@@ -0,0 +1,55 @@
+import { FC } from 'react';
+import { LuHeart, LuZap } from 'react-icons/lu';
+import { LayoutAvatarImageView } from '../../common';
+import { useTarget } from '../../hooks';
+
+/**
+ * Mirror-layout HUD showing the currently-targeted player's stats.
+ * Visible only after the first UpdateTargetStatsEvent packet lands
+ * (on profile-card view of another user). Re-renders on every
+ * subsequent packet so switching targets updates in place.
+ *
+ * Matches .designs/client_ui.pen frame 1hvgH, scaled to the same
+ * 280px width as StatsHUD.
+ */
+export const TargetHUD: FC<{}> = () =>
+{
+    const { target = null } = useTarget();
+
+    if(!target) return null;
+
+    const { stats } = target;
+    const hpPct = stats.maxHp > 0 ? Math.max(0, Math.min(100, (stats.hp / stats.maxHp) * 100)) : 0;
+    const energyPct = stats.maxEnergy > 0 ? Math.max(0, Math.min(100, (stats.energy / stats.maxEnergy) * 100)) : 0;
+
+    return (
+        <div className="pixeltower-stats-hud pixeltower-stats-hud--target">
+            <div className="pixeltower-stats-hud__row">
+                <div className="pixeltower-stats-hud__avatar">
+                    <div className="pixeltower-stats-hud__avatar-inner">
+                        <LayoutAvatarImageView figure={ target.figure } direction={ 4 } />
+                        <div className="pixeltower-stats-hud__name">{ target.userName }</div>
+                    </div>
+                </div>
+                <div className="pixeltower-stats-hud__bars">
+                    <div className="pixeltower-stats-hud__bar pixeltower-stats-hud__bar--hp">
+                        <div className="pixeltower-stats-hud__bar-track" />
+                        <div className="pixeltower-stats-hud__bar-fill pixeltower-stats-hud__bar-fill--hp" style={ { width: `${ hpPct }%` } } />
+                        <div className="pixeltower-stats-hud__bar-content">
+                            <LuHeart className="pixeltower-stats-hud__bar-icon" />
+                            <span className="pixeltower-stats-hud__bar-label">{ stats.hp }/{ stats.maxHp }</span>
+                        </div>
+                    </div>
+                    <div className="pixeltower-stats-hud__bar pixeltower-stats-hud__bar--energy">
+                        <div className="pixeltower-stats-hud__bar-track" />
+                        <div className="pixeltower-stats-hud__bar-fill pixeltower-stats-hud__bar-fill--energy" style={ { width: `${ energyPct }%` } } />
+                        <div className="pixeltower-stats-hud__bar-content">
+                            <LuZap className="pixeltower-stats-hud__bar-icon" />
+                            <span className="pixeltower-stats-hud__bar-label">{ stats.energy }/{ stats.maxEnergy }</span>
+                        </div>
+                    </div>
+                </div>
+            </div>
+        </div>
+    );
+}
diff --git a/src/components/pixeltower/ToolbarHUD.scss b/src/components/pixeltower/ToolbarHUD.scss
new file mode 100644
index 0000000..11e55ad
--- /dev/null
+++ b/src/components/pixeltower/ToolbarHUD.scss
@@ -0,0 +1,226 @@
+@use './tokens' as *;
+
+// Bottom-center toolbar — matches .designs/client_ui.pen frame Y6gyQ.
+// Replaces upstream .nitro-toolbar entirely. Hosts the
+// #toolbar-chat-input-container portal target that ChatInputView
+// writes into.
+
+$pt-toolbar-chat-top:    #D9DFE6;                   // light-bevel top (soft gray)
+$pt-toolbar-chat-bottom: #FFFFFF;                   // light-bevel bottom (white)
+$pt-toolbar-chat-icon:   #3E4E6E;
+$pt-toolbar-chat-placeholder: #6B7A8F;
+$pt-toolbar-chat-text:   #0A1424;                   // dark typed text on light bevel
+
+.pixeltower-toolbar-hud {
+    position: fixed;
+    bottom: 12px;
+    left: 12px;
+    right: 12px;
+    // Above Nitro's global `canvas { z-index: 1000 }` so the HUD isn't
+    // occluded the moment RoomView un-hides. All pixeltower HUDs share
+    // this tier.
+    z-index: 1100;
+
+    display: flex;
+    flex-direction: row;
+    align-items: center;
+    gap: 8px;
+    height: 60px;
+    padding: 8px 12px;
+
+    // The toolbar root is transparent per .designs/client_ui.pen frame
+    // Y6gyQ — no background, border, blur, or rim shadow. The buttons
+    // and chat field carry their own chrome; the container is layout only.
+    background: transparent;
+
+    &__left-group {
+        // Staff-only buttons (Navigator, Shop, Mod Tools) live here;
+        // non-staff render nothing. The group flexes to fill the space
+        // between the toolbar's left edge and the chatfield and
+        // left-aligns its contents (default justify-content), matching
+        // `.pen` Y6gyQ > W0BVS where the buttons sit at the far left.
+        flex: 1 1 0;
+        display: flex;
+        flex-direction: row;
+        align-items: center;
+        gap: 8px;
+        align-self: stretch;
+    }
+
+    &__chat-field {
+        display: flex;
+        flex-direction: row;
+        align-items: center;
+        gap: 8px;
+        width: 420px;
+        max-width: 60%;
+        height: 40px;
+        padding: 8px 12px;
+        border: 2px solid $pt-panel-stroke;
+        border-radius: 3px;
+        // light-bevel: soft-gray top, white bottom — inverse of the dark chip
+        // gradient used on the button groups. Per .pen qkSYj > hKnXO.
+        background: linear-gradient(180deg,
+            $pt-toolbar-chat-top 0%, $pt-toolbar-chat-top 50%,
+            $pt-toolbar-chat-bottom 50%, $pt-toolbar-chat-bottom 100%);
+        box-shadow: 0 -1px 0 #FFFFFF;                // white top-edge highlight per .pen qkSYj > hKnXO
+    }
+
+    &__chat-input-slot {
+        flex: 1 1 0;
+        min-width: 0;
+        display: flex;
+        align-items: center;
+
+        // Placeholder visible only before ChatInputView portals its
+        // DOM into this slot (first frame on mount).
+        &:empty::before {
+            content: 'Say something…';
+            font-family: $pt-font-family;
+            font-size: 14px;
+            font-weight: 700;
+            color: $pt-toolbar-chat-placeholder;
+        }
+
+        // Neutralise the upstream chat-input pill (#EDEDED body, black
+        // border, 8px radius, ::before highlight, overflow:hidden).
+        // Every descendant rule is nested inside this block so the
+        // compiled selector chain is 3 classes deep (specificity 0,3,_)
+        // — upstream's `.nitro-chat-input-container .input-sizer`
+        // (0,2,0) would otherwise win the cascade on offset, silently
+        // reinstating `display: inline-grid` + `padding: 0 10px` and
+        // shoving the placeholder right.
+        .nitro-chat-input-container {
+            flex: 1 1 auto;
+            display: flex;
+            align-items: center;
+            gap: 8px;
+            width: 100%;
+            max-width: 100%;
+            padding: 0;
+            background: transparent;
+            border: 0;
+            border-radius: 0;
+            box-shadow: none;
+            overflow: visible;
+
+            &::before { display: none; }
+
+            // Upstream: display:inline-grid + padding:0 10px. Replace
+            // with flex fill so the placeholder anchors left, right
+            // after the chat-bubble icon.
+            .input-sizer {
+                flex: 1 1 auto;
+                min-width: 0;
+                padding: 0;
+                background: transparent;
+                display: flex;
+                align-items: center;
+
+                &::after { display: none; }
+            }
+
+            .chat-input,
+            input.chat-input {
+                width: 100%;
+                background: transparent;
+                border: 0;
+                outline: none;
+                color: $pt-toolbar-chat-text;
+                font-family: $pt-font-family;
+                font-size: 14px;
+                font-weight: 700;
+                text-align: left;
+
+                &::placeholder { color: $pt-toolbar-chat-placeholder; }
+            }
+
+            // ChatInputStyleSelectorView's trigger icon sits inline;
+            // the pixeltower design doesn't surface chat-style
+            // customisation, so hide the trigger entirely.
+            .chatstyles-icon { display: none; }
+        }
+    }
+
+    &__right-group {
+        // Mirrors the `&__left-group` so the 420px chatfield sits
+        // horizontally centred in the 800px toolbar. The phone button
+        // is right-aligned inside this group, landing at the far right
+        // of the toolbar (matches `.pen` Y6gyQ > 5rdYe justifyContent:end).
+        flex: 1 1 0;
+        display: flex;
+        align-items: center;
+        justify-content: flex-end;
+        height: 100%;
+    }
+
+    // Grouped button panels — matches .designs/client_ui.pen frame qkSYj.
+    // Staff icons (nav/shop/mod) and device icons (phone/settings) live
+    // inside shared chip panels with vertical dividers between slots.
+    &__group {
+        @include pt-chip;                            // chip gradient + 2px stroke + radius 3
+        display: flex;
+        height: 40px;
+        box-shadow: 0 1px 0 #9AB4D3;                 // periwinkle underglow per design
+        overflow: hidden;                            // clip slot hover fills to the chip's rounded corners
+    }
+
+    &__slot {
+        width: 40px;
+        height: 40px;
+        padding: 0;
+        background: transparent;
+        border: 0;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        color: $pt-text;
+        cursor: pointer;
+        transition: background-color 120ms ease, filter 120ms ease;
+        font-family: inherit;
+
+        svg { width: 20px; height: 20px; }
+
+        // Translucent white overlay reads as a visible hover affordance
+        // against the chip gradient; the `__group`'s overflow:hidden
+        // clips it to the chip's rounded corners on edge slots.
+        &:hover {
+            background: rgba(255, 255, 255, 0.12);
+            filter: brightness(1.08);
+        }
+
+        &:active {
+            background: rgba(0, 0, 0, 0.18);
+            filter: none;
+        }
+    }
+
+    &__divider {
+        width: 2px;
+        height: 40px;
+        background: $pt-panel-stroke;
+        flex-shrink: 0;
+        align-self: stretch;
+    }
+
+    // Clickable chat-bubble icon inside the chat field — opens chat history.
+    &__chat-history-btn {
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        padding: 0;
+        background: transparent;
+        border: 0;
+        color: $pt-toolbar-chat-icon;
+        cursor: pointer;
+        transition: filter 120ms ease;
+
+        svg {
+            width: 16px;
+            height: 16px;
+            flex: 0 0 auto;
+        }
+
+        &:hover { filter: brightness(1.3); }
+    }
+}
diff --git a/src/components/pixeltower/ToolbarHUD.tsx b/src/components/pixeltower/ToolbarHUD.tsx
new file mode 100644
index 0000000..ecef69d
--- /dev/null
+++ b/src/components/pixeltower/ToolbarHUD.tsx
@@ -0,0 +1,109 @@
+import { FC } from 'react';
+import { LuCompass, LuMessagesSquare, LuPackage, LuSettings, LuShieldAlert, LuShoppingBag, LuSmartphone } from 'react-icons/lu';
+import { CreateLinkEvent, GetSessionDataManager } from '../../api';
+
+// Bottom-center toolbar replacing upstream ToolbarView. Matches
+// .designs/client_ui.pen frame qkSYj (Toolbar HUD "grouped" variant):
+//   leftGroup  — staffGroup chip holding Navigator + Shop + Inventory +
+//                Mod Tools (staff only) with vertical dividers between slots
+//   chatField  — hosts the #toolbar-chat-input-container portal used
+//                by ChatInputView; the chat-bubble icon on the left of
+//                the field is a button that toggles the chat log
+//   rightGroup — deviceGroup chip holding Phone (friends list) +
+//                Settings with a vertical divider between them
+//
+// Staff gate mirrors upstream ToolbarView: GetSessionDataManager().
+// isModerator is populated from the server's SecureLoginOK payload, so
+// it's stable for the session. Non-staff render nothing in the left
+// group; its flex slot still fills so the chatfield stays centred.
+//
+// Also carries a hidden #toolbar-friend-bar-container portal target —
+// FriendsView portals FriendBarView into it unconditionally, so the
+// slot must exist in the DOM even though the pixeltower design
+// doesn't surface the online-friends pill. Removing it triggers
+// React error #200 ("Target container is not a DOM element") the
+// first time FriendsView renders, which unmounts MainView.
+
+export const ToolbarHUD: FC<{}> = () =>
+{
+    const isMod = GetSessionDataManager().isModerator;
+
+    return (
+        <div className="pixeltower-toolbar-hud">
+            <div className="pixeltower-toolbar-hud__left-group">
+                { isMod && (
+                    <div className="pixeltower-toolbar-hud__group">
+                        <button
+                            type="button"
+                            className="pixeltower-toolbar-hud__slot"
+                            aria-label="Navigator"
+                            title="Navigator"
+                            onClick={ () => CreateLinkEvent('navigator/toggle') }>
+                            <LuCompass />
+                        </button>
+                        <div className="pixeltower-toolbar-hud__divider" />
+                        <button
+                            type="button"
+                            className="pixeltower-toolbar-hud__slot"
+                            aria-label="Shop"
+                            title="Shop"
+                            onClick={ () => CreateLinkEvent('catalog/open') }>
+                            <LuShoppingBag />
+                        </button>
+                        <div className="pixeltower-toolbar-hud__divider" />
+                        <button
+                            type="button"
+                            className="pixeltower-toolbar-hud__slot"
+                            aria-label="Inventory"
+                            title="Inventory"
+                            onClick={ () => CreateLinkEvent('inventory/toggle') }>
+                            <LuPackage />
+                        </button>
+                        <div className="pixeltower-toolbar-hud__divider" />
+                        <button
+                            type="button"
+                            className="pixeltower-toolbar-hud__slot"
+                            aria-label="Mod tools"
+                            title="Mod tools"
+                            onClick={ () => CreateLinkEvent('mod-tools/toggle') }>
+                            <LuShieldAlert />
+                        </button>
+                    </div>
+                ) }
+            </div>
+            <div className="pixeltower-toolbar-hud__chat-field">
+                <button
+                    type="button"
+                    className="pixeltower-toolbar-hud__chat-history-btn"
+                    aria-label="Chat history"
+                    title="Chat history"
+                    onClick={ () => CreateLinkEvent('chat-history/toggle') }>
+                    <LuMessagesSquare />
+                </button>
+                <div id="toolbar-chat-input-container" className="pixeltower-toolbar-hud__chat-input-slot" />
+            </div>
+            <div className="pixeltower-toolbar-hud__right-group">
+                <div className="pixeltower-toolbar-hud__group">
+                    <button
+                        type="button"
+                        className="pixeltower-toolbar-hud__slot"
+                        aria-label="Friends list"
+                        title="Friends"
+                        onClick={ () => CreateLinkEvent('friends/toggle') }>
+                        <LuSmartphone />
+                    </button>
+                    <div className="pixeltower-toolbar-hud__divider" />
+                    <button
+                        type="button"
+                        className="pixeltower-toolbar-hud__slot"
+                        aria-label="Settings"
+                        title="Settings"
+                        onClick={ () => CreateLinkEvent('user-settings/toggle') }>
+                        <LuSettings />
+                    </button>
+                </div>
+            </div>
+            <div id="toolbar-friend-bar-container" style={ { display: 'none' } } />
+        </div>
+    );
+}
diff --git a/src/components/pixeltower/_app-overrides.scss b/src/components/pixeltower/_app-overrides.scss
new file mode 100644
index 0000000..2c6ae63
--- /dev/null
+++ b/src/components/pixeltower/_app-overrides.scss
@@ -0,0 +1,61 @@
+@use './tokens' as *;
+
+// Nitro's Bootstrap fork sets $body-bg: $black (see
+// assets/styles/bootstrap/_variables.scss), and upstream relied on
+// HotelView painting a full-viewport background over that black until
+// a room loaded. Pixeltower removes HotelView (users are pinned to a
+// room on login), so the black body shows through whenever the PIXI
+// canvas hasn't painted room pixels yet — both on first mount and for
+// a few frames during every room swap. Paint a slate-blue fallback
+// that matches the HUD panel language so those gap frames feel
+// intentional instead of looking like a crash.
+
+:root {
+    --bs-body-bg: #{$pt-panel-stroke};
+}
+
+body {
+    background: linear-gradient(180deg, #1a2436 0%, $pt-panel-stroke 100%);
+}
+
+// LoadingView.scss paints $primary teal over the body — drop that so the
+// slate-blue body gradient shows through and the preloader matches the
+// room-swap fallback.
+.nitro-loading {
+    background: transparent;
+}
+
+// Standard Nitro card windows (Friends, Navigator, alert popups, etc.)
+// ship with a flat $primary teal header (#1E7295) for theme-primary and
+// a tertiary/quaternary teal gradient for theme-primary-slim — both
+// clash with the pixeltower HUD's slate-blue chip language. Restyle
+// both header variants with the same hard 50/50 bevel + panel stroke
+// every HUD chip uses so dialog windows read as the same family as the
+// toolbar. Close button stays upstream red on purpose.
+.nitro-card.theme-primary .nitro-card-header,
+.nitro-card.theme-primary-slim .nitro-card-header {
+    background: linear-gradient(180deg,
+        $pt-chip-top 0%, $pt-chip-top 50%,
+        $pt-chip-bottom 50%, $pt-chip-bottom 100%);
+    border-bottom: 2px solid $pt-panel-stroke;
+}
+
+// Drop the slim variant's white underline (box-shadow) and inner-top
+// white highlight (:before) — both are baked bevels that fight the
+// flat 2D chip gradient.
+.nitro-card.theme-primary-slim .nitro-card-header {
+    box-shadow: none;
+}
+
+.nitro-card.theme-primary-slim .nitro-card-header:before {
+    display: none;
+}
+
+// Hide the floating name-tag pill above avatars (own user, friends,
+// anyone else) — AvatarInfoWidgetNameView always applies `.name-only`
+// on the ContextMenuView, so this selector catches every variant.
+// The click-to-open avatar info card is a different rendering path
+// and stays untouched.
+.nitro-context-menu.name-only {
+    display: none;
+}
diff --git a/src/components/pixeltower/_tokens.scss b/src/components/pixeltower/_tokens.scss
new file mode 100644
index 0000000..1bc5a1d
--- /dev/null
+++ b/src/components/pixeltower/_tokens.scss
@@ -0,0 +1,85 @@
+// Shared design tokens + mixins for pixeltower HUDs.
+// Palette extracted from .designs/client_ui.pen — all four HUD frames
+// (Player HzOI4, Target 1hvgH, Status KVhc5, Drawer tW1BU) share the same
+// chunky 2D-bevel language: translucent slate-blue panel + 2px near-navy
+// outline + hard 50/50 two-tone gradient on interactive chips.
+
+// ──────────── panel (outer translucent shell) ────────────
+$pt-panel-fill:        rgba(62, 78, 110, 0.50);   // #3E4E6E80
+$pt-panel-stroke:      #0A1424;
+$pt-panel-shadow-drop: rgba(0, 0, 0, 0.44);
+$pt-panel-shadow-rim:  #7A95B8;
+$pt-panel-blur:        8px;
+
+// ──────────── chip / button / row (inner bevel) ────────────
+$pt-chip-top:    #5E7FA3;
+$pt-chip-bottom: #2B3A54;
+
+// ──────────── HP bar ────────────
+$pt-hp-top:     #F04848;
+$pt-hp-bottom:  #8E1414;
+$pt-hp-stroke:  #2A0404;
+$pt-hp-glow:    #FFB0B0;
+$pt-hp-track-top:    #3a0f0c;
+$pt-hp-track-bottom: #1e0706;
+
+// ──────────── Energy bar ────────────
+$pt-energy-top:     #4E85E0;
+$pt-energy-bottom:  #143972;
+$pt-energy-stroke:  #050F22;
+$pt-energy-glow:    #A8C4EE;
+$pt-energy-track-top:    #0f2442;
+$pt-energy-track-bottom: #06121f;
+
+// ──────────── Avatar ────────────
+$pt-avatar-frame:      #FFD028;
+$pt-avatar-frame-glow: #FFF4B0;
+$pt-avatar-inner-top:    #6D7E90;
+$pt-avatar-inner-bottom: #495565;
+
+// ──────────── Wanted stars ────────────
+$pt-wanted-inactive:   #374151;
+$pt-wanted-active:     #FFD028;
+$pt-wanted-active-glow: #FFF4B0;
+
+// ──────────── Badges / accents ────────────
+$pt-username-badge:        #0A1424;
+$pt-username-badge-target: rgba(0, 0, 0, 0.60);   // #00000099
+$pt-vip-top:      #FFE066;                        // VIP badge gradient top half
+$pt-vip-bottom:   #D99A00;                        // VIP badge gradient bottom half
+$pt-vip-glow:     #FFF8C8;                        // VIP badge 1px underglow
+$pt-vip-text:     #2A1A00;                        // dark warm brown on yellow bevel
+$pt-coin-yellow:  #FFC700;
+$pt-diamond-blue: #3E8FD8;
+
+$pt-text:       #FFFFFF;
+
+// Ubuntu is loaded via nitro/src/assets/styles/fonts.scss from
+// Ubuntu-C.ttf — the Condensed variant. Use this token across every
+// pixeltower HUD so the .pen designs (which reference system fonts
+// like Inter) always render in Ubuntu Condensed in-game.
+$pt-font-family: Ubuntu, sans-serif;
+
+// ──────────── mixins ────────────
+
+// Translucent slate-blue shell with 2px outline, blur, stacked drop+rim shadow.
+@mixin pt-panel {
+    background: $pt-panel-fill;
+    border: 2px solid $pt-panel-stroke;
+    border-radius: 7px;
+    backdrop-filter: blur($pt-panel-blur);
+    -webkit-backdrop-filter: blur($pt-panel-blur);
+    box-shadow:
+        0 3px 0 $pt-panel-shadow-drop,
+        0 2px 0 $pt-panel-shadow-rim;
+}
+
+// Hard 50/50 two-tone bevel gradient with 2px outline — used on every
+// button, row, and inner chip across all four HUDs.
+@mixin pt-chip {
+    background: linear-gradient(180deg,
+        $pt-chip-top 0%, $pt-chip-top 50%,
+        $pt-chip-bottom 50%, $pt-chip-bottom 100%);
+    border: 2px solid $pt-panel-stroke;
+    border-radius: 5px;
+}
diff --git a/src/components/right-side/RightSideView.scss b/src/components/right-side/RightSideView.scss
index a597741..6363432 100644
--- a/src/components/right-side/RightSideView.scss
+++ b/src/components/right-side/RightSideView.scss
@@ -1,10 +1,10 @@
 .nitro-right-side {
     position: absolute;
-    top: 0px;
+    top: 132px;                                    // clear the Status HUD (top:12 + ~112 hud height + 8 gap)
     right: 10px;
     min-width: 200px;
     max-width: 200px;
-    height: calc(100% - #{$toolbar-height});
+    height: calc(100% - #{$toolbar-height} - 132px);
     z-index: $rightside-zindex;
     pointer-events: none;
 }
diff --git a/src/components/right-side/RightSideView.tsx b/src/components/right-side/RightSideView.tsx
index 92e8129..89a74b4 100644
--- a/src/components/right-side/RightSideView.tsx
+++ b/src/components/right-side/RightSideView.tsx
@@ -3,7 +3,6 @@ import { Column } from '../../common';
 import { OfferView } from '../catalog/views/targeted-offer/OfferView';
 import { GroupRoomInformationView } from '../groups/views/GroupRoomInformationView';
 import { NotificationCenterView } from '../notification-center/NotificationCenterView';
-import { PurseView } from '../purse/PurseView';
 import { MysteryBoxExtensionView } from '../room/widgets/mysterybox/MysteryBoxExtensionView';
 import { RoomPromotesWidgetView } from '../room/widgets/room-promotes/RoomPromotesWidgetView';
 
@@ -12,7 +11,6 @@ export const RightSideView: FC<{}> = props =>
     return (
         <div className="nitro-right-side">
             <Column position="relative" gap={ 1 }>
-                <PurseView />
                 <GroupRoomInformationView />
                 <MysteryBoxExtensionView />
                 <OfferView/>
diff --git a/src/components/room/widgets/RoomWidgetsView.tsx b/src/components/room/widgets/RoomWidgetsView.tsx
index c220299..19ede3c 100644
--- a/src/components/room/widgets/RoomWidgetsView.tsx
+++ b/src/components/room/widgets/RoomWidgetsView.tsx
@@ -13,7 +13,6 @@ import { FurnitureWidgetsView } from './furniture/FurnitureWidgetsView';
 import { PetPackageWidgetView } from './pet-package/PetPackageWidgetView';
 import { RoomFilterWordsWidgetView } from './room-filter-words/RoomFilterWordsWidgetView';
 import { RoomThumbnailWidgetView } from './room-thumbnail/RoomThumbnailWidgetView';
-import { RoomToolsWidgetView } from './room-tools/RoomToolsWidgetView';
 import { WordQuizWidgetView } from './word-quiz/WordQuizWidgetView';
 
 export const RoomWidgetsView: FC<{}> = props =>
@@ -159,7 +158,7 @@ export const RoomWidgetsView: FC<{}> = props =>
             <ChatInputView />
             <DoorbellWidgetView />
             <FurnitureWidgetsView />
-            <RoomToolsWidgetView />
+            { /* RoomToolsWidgetView unmounted — pixeltower removes the bottom-left gear/zoom/chat-history sidebar. */ }
             <RoomFilterWordsWidgetView />
             <RoomThumbnailWidgetView />
             <FurniChooserWidgetView />
diff --git a/src/components/room/widgets/avatar-info/AvatarInfoWidgetView.tsx b/src/components/room/widgets/avatar-info/AvatarInfoWidgetView.tsx
index 5a0acf1..3cea60e 100644
--- a/src/components/room/widgets/avatar-info/AvatarInfoWidgetView.tsx
+++ b/src/components/room/widgets/avatar-info/AvatarInfoWidgetView.tsx
@@ -104,6 +104,7 @@ export const AvatarInfoWidgetView: FC<{}> = props =>
         switch(avatarInfo.type)
         {
             case AvatarInfoFurni.FURNI:
+                if(!GetSessionDataManager().isModerator && !(roomSession?.isRoomOwner)) return null;
                 return <InfoStandWidgetFurniView avatarInfo={ (avatarInfo as AvatarInfoFurni) } onClose={ () => setAvatarInfo(null) } />;
             case AvatarInfoUser.OWN_USER:
             case AvatarInfoUser.PEER:
diff --git a/src/components/room/widgets/avatar-info/menu/AvatarInfoWidgetOwnAvatarView.tsx b/src/components/room/widgets/avatar-info/menu/AvatarInfoWidgetOwnAvatarView.tsx
index c72e269..f5e66cf 100644
--- a/src/components/room/widgets/avatar-info/menu/AvatarInfoWidgetOwnAvatarView.tsx
+++ b/src/components/room/widgets/avatar-info/menu/AvatarInfoWidgetOwnAvatarView.tsx
@@ -130,9 +130,6 @@ export const AvatarInfoWidgetOwnAvatarView: FC<AvatarInfoWidgetOwnAvatarViewProp
                         <ContextMenuListItemView onClick={ event => processAction('decorate') }>
                             { LocalizeText('widget.avatar.decorate') }
                         </ContextMenuListItemView> }
-                    <ContextMenuListItemView onClick={ event => processAction('change_looks') }>
-                        { LocalizeText('widget.memenu.myclothes') }
-                    </ContextMenuListItemView>
                     { (HasHabboClub() && !isRidingHorse) &&
                         <ContextMenuListItemView onClick={ event => processAction('dance_menu') }>
                             <FaChevronRight className="right fa-icon" />
@@ -150,10 +147,6 @@ export const AvatarInfoWidgetOwnAvatarView: FC<AvatarInfoWidgetOwnAvatarViewProp
                         <FaChevronRight className="right fa-icon" />
                         { LocalizeText('infostand.link.expressions') }
                     </ContextMenuListItemView>
-                    <ContextMenuListItemView onClick={ event => processAction('signs') }>
-                        <FaChevronRight className="right fa-icon" />
-                        { LocalizeText('infostand.show.signs') }
-                    </ContextMenuListItemView>
                     { (avatarInfo.carryItem > 0) &&
                         <ContextMenuListItemView onClick={ event => processAction('drop_carry_item') }>
                             { LocalizeText('avatar.widget.drop_hand_item') }
diff --git a/src/hooks/index.ts b/src/hooks/index.ts
index a551574..0de8786 100644
--- a/src/hooks/index.ts
+++ b/src/hooks/index.ts
@@ -13,6 +13,7 @@ export * from './inventory';
 export * from './mod-tools';
 export * from './navigator';
 export * from './notification';
+export * from './pixeltower';
 export * from './purse';
 export * from './rooms';
 export * from './session';
diff --git a/src/hooks/notification/useNotification.ts b/src/hooks/notification/useNotification.ts
index 0089b86..7ae7b0b 100644
--- a/src/hooks/notification/useNotification.ts
+++ b/src/hooks/notification/useNotification.ts
@@ -1,4 +1,4 @@
-import { AchievementNotificationMessageEvent, ActivityPointNotificationMessageEvent, ClubGiftNotificationEvent, ClubGiftSelectedEvent, ConnectionErrorEvent, HabboBroadcastMessageEvent, HotelClosedAndOpensEvent, HotelClosesAndWillOpenAtEvent, HotelWillCloseInMinutesEvent, InfoFeedEnableMessageEvent, MaintenanceStatusMessageEvent, ModeratorCautionEvent, ModeratorMessageEvent, MOTDNotificationEvent, NotificationDialogMessageEvent, PetLevelNotificationEvent, PetReceivedMessageEvent, RespectReceivedEvent, RoomEnterEffect, RoomEnterEvent, SimpleAlertMessageEvent, UserBannedMessageEvent, Vector3d } from '@nitrots/nitro-renderer';
+import { AchievementNotificationMessageEvent, ActivityPointNotificationMessageEvent, ClubGiftNotificationEvent, ClubGiftSelectedEvent, ConnectionErrorEvent, HabboBroadcastMessageEvent, HotelClosedAndOpensEvent, HotelClosesAndWillOpenAtEvent, HotelWillCloseInMinutesEvent, InfoFeedEnableMessageEvent, MaintenanceStatusMessageEvent, ModeratorCautionEvent, ModeratorMessageEvent, MOTDNotificationEvent, NotificationDialogMessageEvent, PetLevelNotificationEvent, PetReceivedMessageEvent, RespectReceivedEvent, SimpleAlertMessageEvent, UserBannedMessageEvent, Vector3d } from '@nitrots/nitro-renderer';
 import { useCallback, useState } from 'react';
 import { useBetween } from 'use-between';
 import { GetConfiguration, GetNitroInstance, GetRoomEngine, GetSessionDataManager, LocalizeBadgeName, LocalizeText, NotificationAlertItem, NotificationAlertType, NotificationBubbleItem, NotificationBubbleType, NotificationConfirmItem, PlaySound, ProductImageUtility, TradingNotificationType } from '../../api';
@@ -13,15 +13,12 @@ const getTimeZeroPadded = (time: number) =>
     return text.substr((text.length - 2), text.length);
 }
 
-let modDisclaimerTimeout: ReturnType<typeof setTimeout> = null;
-
 const useNotificationState = () =>
 {
     const [ alerts, setAlerts ] = useState<NotificationAlertItem[]>([]);
     const [ bubbleAlerts, setBubbleAlerts ] = useState<NotificationBubbleItem[]>([]);
     const [ confirms, setConfirms ] = useState<NotificationConfirmItem[]>([]);
     const [ bubblesDisabled, setBubblesDisabled ] = useState(false);
-    const [ modDisclaimerShown, setModDisclaimerShown ] = useState(false);
 
     const getMainNotificationConfig = () => GetConfiguration<{ [key: string]: { delivery?: string, display?: string; title?: string; image?: string }}>('notification', {});
 
@@ -396,36 +393,6 @@ const useNotificationState = () =>
         simpleAlert(LocalizeText(parser.alertMessage), NotificationAlertType.DEFAULT, null, null, LocalizeText(parser.titleMessage ? parser.titleMessage : 'notifications.broadcast.title'));
     });
 
-    const onRoomEnterEvent = useCallback(() =>
-    {
-        if(modDisclaimerShown) return;
-
-        if(RoomEnterEffect.isRunning())
-        {
-            if(modDisclaimerTimeout) return;
-
-            modDisclaimerTimeout = setTimeout(() =>
-            {
-                onRoomEnterEvent();
-            }, (RoomEnterEffect.totalRunningTime + 5000));
-        }
-        else
-        {
-            if(modDisclaimerTimeout)
-            {
-                clearTimeout(modDisclaimerTimeout);
-
-                modDisclaimerTimeout = null;
-            }
-
-            showSingleBubble(LocalizeText('mod.chatdisclaimer'), NotificationBubbleType.INFO);
-
-            setModDisclaimerShown(true);
-        }
-    }, [ modDisclaimerShown, showSingleBubble ]);
-
-    useMessageEvent<RoomEnterEvent>(RoomEnterEvent, onRoomEnterEvent);
-
     return { alerts, bubbleAlerts, confirms, simpleAlert, showNitroAlert, showTradeAlert, showConfirm, showSingleBubble, closeAlert, closeBubbleAlert, closeConfirm };
 }
 
diff --git a/src/hooks/pixeltower/index.ts b/src/hooks/pixeltower/index.ts
new file mode 100644
index 0000000..db33e4d
--- /dev/null
+++ b/src/hooks/pixeltower/index.ts
@@ -0,0 +1,3 @@
+export * from './useEmployment';
+export * from './useStats';
+export * from './useTarget';
diff --git a/src/hooks/pixeltower/useEmployment.ts b/src/hooks/pixeltower/useEmployment.ts
new file mode 100644
index 0000000..4907260
--- /dev/null
+++ b/src/hooks/pixeltower/useEmployment.ts
@@ -0,0 +1,11 @@
+// Deferred: the pixeltower-rp plugin doesn't send an employment status
+// message to the client yet. Returning a stable non-employed state
+// keeps the Commands List's "Job" tab hidden for everyone until the
+// server signal lands, at which point this hook will start listening
+// for UpdateEmploymentStatusEvent (or similar) and flip the flag.
+
+export interface EmploymentState {
+    isEmployed: boolean;
+}
+
+export const useEmployment = (): EmploymentState => ({ isEmployed: false });
diff --git a/src/hooks/pixeltower/useStats.ts b/src/hooks/pixeltower/useStats.ts
new file mode 100644
index 0000000..633918d
--- /dev/null
+++ b/src/hooks/pixeltower/useStats.ts
@@ -0,0 +1,44 @@
+import { useState } from 'react';
+import { useBetween } from 'use-between';
+import { UpdatePlayerStatsEvent } from '../../api/pixeltower/PixeltowerSoundMessages';
+import { useMessageEvent } from '../events';
+
+export interface PixeltowerStats
+{
+    hp: number;
+    maxHp: number;
+    energy: number;
+    maxEnergy: number;
+    level: number;
+    xp: number;
+    skillPointsUnspent: number;
+    skillHit: number;
+    skillEndurance: number;
+    skillStamina: number;
+}
+
+const useStatsState = () =>
+{
+    const [ stats, setStats ] = useState<PixeltowerStats>(null);
+
+    useMessageEvent<UpdatePlayerStatsEvent>(UpdatePlayerStatsEvent, event =>
+    {
+        const parser = event.getParser();
+        setStats({
+            hp: parser.hp,
+            maxHp: parser.maxHp,
+            energy: parser.energy,
+            maxEnergy: parser.maxEnergy,
+            level: parser.level,
+            xp: parser.xp,
+            skillPointsUnspent: parser.skillPointsUnspent,
+            skillHit: parser.skillHit,
+            skillEndurance: parser.skillEndurance,
+            skillStamina: parser.skillStamina
+        });
+    });
+
+    return { stats };
+}
+
+export const useStats = () => useBetween(useStatsState);
diff --git a/src/hooks/pixeltower/useTarget.ts b/src/hooks/pixeltower/useTarget.ts
new file mode 100644
index 0000000..62148a2
--- /dev/null
+++ b/src/hooks/pixeltower/useTarget.ts
@@ -0,0 +1,44 @@
+import { useState } from 'react';
+import { useBetween } from 'use-between';
+import { UpdateTargetStatsEvent } from '../../api/pixeltower/PixeltowerSoundMessages';
+import { useMessageEvent } from '../events';
+import { PixeltowerStats } from './useStats';
+
+export interface PixeltowerTarget
+{
+    habboId: number;
+    figure: string;
+    userName: string;
+    stats: PixeltowerStats;
+}
+
+const useTargetState = () =>
+{
+    const [ target, setTarget ] = useState<PixeltowerTarget>(null);
+
+    useMessageEvent<UpdateTargetStatsEvent>(UpdateTargetStatsEvent, event =>
+    {
+        const parser = event.getParser();
+        setTarget({
+            habboId: parser.habboId,
+            figure: parser.figure,
+            userName: parser.userName,
+            stats: {
+                hp: parser.hp,
+                maxHp: parser.maxHp,
+                energy: parser.energy,
+                maxEnergy: parser.maxEnergy,
+                level: parser.level,
+                xp: parser.xp,
+                skillPointsUnspent: parser.skillPointsUnspent,
+                skillHit: parser.skillHit,
+                skillEndurance: parser.skillEndurance,
+                skillStamina: parser.skillStamina
+            }
+        });
+    });
+
+    return { target };
+}
+
+export const useTarget = () => useBetween(useTargetState);
diff --git a/src/hooks/purse/usePurse.ts b/src/hooks/purse/usePurse.ts
index 6bb5a54..d91dc5a 100644
--- a/src/hooks/purse/usePurse.ts
+++ b/src/hooks/purse/usePurse.ts
@@ -2,6 +2,7 @@ import { ActivityPointNotificationMessageEvent, UserCreditsEvent, UserCurrencyCo
 import { useEffect, useMemo, useState } from 'react';
 import { useBetween } from 'use-between';
 import { CloneObject, ClubStatus, GetConfiguration, IPurse, PlaySound, Purse, SendMessageComposer, SoundNames } from '../../api';
+import { NitroPlaySoundEvent } from '../../api/pixeltower/PixeltowerSoundMessages';
 import { useMessageEvent } from '../events';
 
 const usePurseState = () =>
@@ -80,6 +81,13 @@ const usePurseState = () =>
         });
     });
 
+    useMessageEvent<NitroPlaySoundEvent>(NitroPlaySoundEvent, event =>
+    {
+        const sample = event.getParser().sample;
+        console.log('[PixeltowerRP] NitroPlaySoundEvent received, sample=', sample);
+        if(sample) PlaySound(sample);
+    });
+
     useMessageEvent<UserSubscriptionEvent>(UserSubscriptionEvent, event =>
     {
         const parser = event.getParser();
diff --git a/src/hooks/rooms/widgets/useAvatarInfoWidget.ts b/src/hooks/rooms/widgets/useAvatarInfoWidget.ts
index 9545362..1fd4489 100644
--- a/src/hooks/rooms/widgets/useAvatarInfoWidget.ts
+++ b/src/hooks/rooms/widgets/useAvatarInfoWidget.ts
@@ -1,6 +1,7 @@
 import { RoomEngineObjectEvent, RoomEngineUseProductEvent, RoomObjectCategory, RoomObjectType, RoomObjectVariable, RoomSessionPetInfoUpdateEvent, RoomSessionPetStatusUpdateEvent, RoomSessionUserDataUpdateEvent } from '@nitrots/nitro-renderer';
 import { useEffect, useState } from 'react';
-import { AvatarInfoFurni, AvatarInfoName, AvatarInfoPet, AvatarInfoRentableBot, AvatarInfoUser, AvatarInfoUtilities, CanManipulateFurniture, FurniCategory, GetRoomEngine, GetSessionDataManager, IAvatarInfo, IsOwnerOfFurniture, RoomWidgetUpdateRoomObjectEvent, UseProductItem } from '../../../api';
+import { AvatarInfoFurni, AvatarInfoName, AvatarInfoPet, AvatarInfoRentableBot, AvatarInfoUser, AvatarInfoUtilities, CanManipulateFurniture, FurniCategory, GetRoomEngine, GetSessionDataManager, IAvatarInfo, IsOwnerOfFurniture, RoomWidgetUpdateRoomObjectEvent, SendMessageComposer, UseProductItem } from '../../../api';
+import { PixeltowerSetTargetComposer } from '../../../api/pixeltower/PixeltowerSoundMessages';
 import { useRoomEngineEvent, useRoomSessionManagerEvent, useUiEvent } from '../../events';
 import { useFriends } from '../../friends';
 import { useWired } from '../../wired';
@@ -87,6 +88,11 @@ const useAvatarInfoWidgetState = () =>
                         break;
                     case RoomObjectType.USER:
                         info = AvatarInfoUtilities.getUserInfo(category, userData);
+                        // pixeltower: avatar-sprite click drives the TargetHUD.
+                        // Skip self-selection — server also clears on self, but
+                        // we don't want to burn a round-trip just to no-op.
+                        if(userData.webID !== GetSessionDataManager().userId)
+                            SendMessageComposer(new PixeltowerSetTargetComposer(userData.webID));
                         break;
                     case RoomObjectType.BOT:
                         info = AvatarInfoUtilities.getBotInfo(category, userData);
diff --git a/src/hooks/rooms/widgets/useChatInputWidget.ts b/src/hooks/rooms/widgets/useChatInputWidget.ts
index 5b11ecf..b4c092a 100644
--- a/src/hooks/rooms/widgets/useChatInputWidget.ts
+++ b/src/hooks/rooms/widgets/useChatInputWidget.ts
@@ -170,6 +170,24 @@ const useChatInputWidgetState = () =>
                         SendMessageComposer(new RoomSettingsComposer(roomSession.roomId));
                     }
 
+                    return null;
+                case ':roomsettings':
+                    if(GetSessionDataManager().isModerator)
+                    {
+                        SendMessageComposer(new RoomSettingsComposer(roomSession.roomId));
+                    }
+
+                    return null;
+                case ':floorplan':
+                    if(GetSessionDataManager().isModerator)
+                    {
+                        CreateLinkEvent('floor-editor/toggle');
+                    }
+
+                    return null;
+                case ':commands':
+                    CreateLinkEvent('commands-list/toggle');
+
                     return null;
             }
         }
diff --git a/vite.config.js b/vite.config.js
index 1c4696c..e05278a 100644
--- a/vite.config.js
+++ b/vite.config.js
@@ -4,6 +4,7 @@ import { resolve } from 'path';
 import { defineConfig } from 'vite';
 
 export default defineConfig({
+    base: '/client/',
     plugins: [ react() ],
     resolve: {
         alias: {
