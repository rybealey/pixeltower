diff --git a/src/main/java/com/eu/habbo/habbohotel/rooms/RoomUnit.java b/src/main/java/com/eu/habbo/habbohotel/rooms/RoomUnit.java
--- a/src/main/java/com/eu/habbo/habbohotel/rooms/RoomUnit.java
+++ b/src/main/java/com/eu/habbo/habbohotel/rooms/RoomUnit.java
@@ -356,20 +356,13 @@
       this.setCurrentLocation(room.getLayout().getTile(next.x, next.y));
       this.resetIdleTimer();
 
-      if (habbo != null) {
-        HabboItem topItem = room.getTopItemAt(next.x, next.y);
-
-        boolean isAtDoor =
-            next.x == room.getLayout().getDoorX() && next.y == room.getLayout().getDoorY();
-        boolean publicRoomKicks = !room.isPublicRoom() || Emulator.getConfig()
-            .getBoolean("hotel.room.public.doortile.kick");
-        boolean invalidated = topItem != null && topItem.invalidatesToRoomKick();
-
-        if (this.canLeaveRoomByDoor && isAtDoor && publicRoomKicks && !invalidated) {
-          Emulator.getThreading().run(new RoomUnitKick(habbo, room, false), 500);
-        }
-      }
+      // pixeltower: door-tile kick removed — the door is entry-only,
+      // never an exit. Upstream Arcturus scheduled a RoomUnitKick when
+      // a user stepped onto the door tile, which disposes them to
+      // hotel view. The pixeltower Nitro build strips HotelView
+      // entirely, so the kick would drop the user into a blank-screen
+      // limbo. Users now simply stand on the tile with no side-effect.
 
       return false;
 
     } catch (Exception e) {
